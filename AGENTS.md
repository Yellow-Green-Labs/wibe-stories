THIS FILE IS THE RULE OF LAW FOR THIS PROJECT

This file is automatically loaded into every agent session. These are MANDATORY CONSTRAINTS — ignoring or skipping them is a violation. Your work is not complete until every applicable rule here has been followed.

YOU ARE BOUND BY THE CONTENTS OF THIS FILE. FULL STOP.

YOU ARE NOT DONE UNTIL THIS FILE IS UPDATED. Before ending your session, verify this file reflects all changes you made. If it doesn't, update it or your work is incomplete.

# Foundational principle — read first, overrides everything

> **"Fall in love with the problem, not the solution."** — Tanay Kothari, CEO at Wispr Flow

> **"We hire for a rare intersection of people who are aggressive about outcomes but kind and direct in how they communicate."** — Sahaj Garg, Co-Founder/CTO @ Wispr Flow

- **Name the problem before building.** If you can't state the real user problem, don't build it.
- **"I love this, so I built it" is not a reason.** Purpose is. Build for an unmet need, not attachment.
- **The solution is disposable.** A better path beats the current one — drop it without ego.
- **Positioning leads with the problem**, never fondness for the product or brand.

# Mandatory rules

- **Audience lens**: Target users span 20s–70s (friends, family, relatives, parents, grandparents). Every decision must serve ALL of them.
- **Tanay lens**: Before proposing any change, read `docs/Tanay-linkedin-posts.md`. Align with or explicitly justify deviations from Wispr Flow CEO's thinking.
- **Codebase lens**: Read/understand the full project implementation before proposing features or improvements. No blind suggestions.
- Every recommendation, explanation, or answer must include a **confidence level: High, Medium, or Low**. If **Low**, stop and ask for clarification before proceeding.
- If anything is unclear, ask explicitly — never assume or guess.
- Delegate smaller/parallel tasks to subagents aggressively.
- Start with **Explore** (read-only) when uncertain; switch to **General** only when a bounded task is clear.
- Keep all changes reversible when possible.
- **Documentation truth rule (public docs):** `docs-src/` must state *what and why*, never *how* — omit service names (Redis, Neon, Postgres, Vercel Blob), storage keys, session mechanics, rate limits, internal endpoints, cron schedules, exact thresholds, and file paths. If a public doc needs to reference an internal detail, link to the internal file instead.
- **Documentation truth rule (internal docs):** `docs-internal/` must be 100% truthful with no gaps. Every constraint, mechanism, and threshold that exists in the code must be documented somewhere in `docs-internal/`.
- **Leak detection**: Before writing or editing any public doc, cross-check every number, name, and mechanism against the leak list above. If uncertain, move the detail to `docs-internal/` and link.

## MANDATORY: Self-update rule

1. **When you modify project files, you MUST also update this file** to reflect the new state. This is not optional. There is no exception.
2. **When in doubt, update anyway.** Stale documentation is worse than no documentation.
3. **Run `@agsync` at end of each session** to record changes. This automatically updates CHANGELOG.md and this file. The agsync comment block keeps only the primary last-run note plus the ~3 most recent session notes — older notes are archived verbatim to `docs-internal/agsync-archive.md` (do not let the block regrow).
4. **YOU ARE NOT DONE UNTIL THIS FILE IS UPDATED.** Before ending your session, verify this file reflects all changes you made. If it doesn't, update it or your work is incomplete.

# Session-end verification checklist

At the end of every session that touched project files, you MUST mentally verify each:

- [x] Did I run `@agsync` to record this session's changes?
- [x] Did I update "Key files" if I added/removed/renamed files?
- [x] Did I update "Known bugs" if I fixed or introduced bugs?
- [x] Did I update any other section that my changes affect?

If any box is unchecked, you are not done. Fix it.

# Known bugs

- **Firefox** — Web Speech API unsupported; no voice recording, no in-app fallback. Users can still type their message. (The `ffNotice` i18n key and `showNotice("firefox")` caller were removed in v0.11.0.0 as dead code; no Firefox-specific toast is shown anymore.)
- **resetBtn async** — `stopDeepgramRecording()` is async; `finishRec()` runs after state updates, causing brief flicker.
- **Voice-attached cards** — WebM via `canvas.captureStream()` + `MediaRecorder`; Safari unsupported; iOS no native .webm playback.
- **Counter stuck at 5/5 on Vercel** — `reportRecordingDuration()` had an early return on 0-duration (fixed in v0.10.4.4), but counter still showed 5/5 after multiple recordings. v0.10.4.5 added `_refreshLimitsFromServer()` for cap-recoveries; v0.10.4.7 adds re-fetch after **every** `reportRecordingDuration` so client always mirrors server state. Monotonic guard in `updateRecCounter(used, max, cumulativeUsed, cumulativeMax, sessionId)` ignores stale `used < _lastKnownRecordingsUsed` values and resets on session-change or day-rollover, so out-of-order safety-net responses from prior recordings can't make the counter go backwards. The 4× `console.debug` instrumentation remains for future root-cause diagnosis if the symptom recurs.

# SEO & Security configuration

A full Content-Security-Policy **is** set in `vercel.json` (allows `unpkg.com`, Google Fonts, Vercel Blob, Sentry CDN, and Sentry ingest). It does **not** break the Web Speech API, which is browser-native and needs no `connect-src` entry. All other security headers are in `vercel.json` too.

**Clerk auth wiring (2026-08):** official 3-part script init in `wisprstories.html` + `pricing.html` — `@clerk/ui@1/dist/ui.browser.js` + `clerk-js@6/dist/clerk.browser.js` (with `data-clerk-publishable-key` = pk_live, `data-clerk-proxy-url="/__clerk"`) + explicit `Clerk.load({ ui: { ClerkUI: window.__internal_ClerkUICtor } })`; omitting the UI bundle or the load() option throws "Clerk was not loaded with Ui components". CSP allows `*.clerk.accounts.dev`/`*.clerk.com` in script-src/connect-src/frame-src (+ img.clerk.com), plus `https://challenges.cloudflare.com` + `https://*.protect.clerk.com` in script-src/frame-src and `https://*.protect.clerk.com:*` in connect-src (Clerk CAPTCHA + abuse/fraud protection — required for sign-up/sign-in to work). Clerk is in **proxy mode**: a top-level edge function `api/clerk-proxy.js` forwards `/__clerk/:path*` (via `?clerkPath=` query param) to `frontend-api.clerk.dev` with `Clerk-Proxy-Url` + `Clerk-Secret-Key` headers; `redirect:'manual'` passes Clerk's 307 version-redirects back so the browser re-enters the proxy at the exact-version path. The proxy must be enabled in the Clerk Dashboard (Domains → Frontend API → Set proxy configuration → `https://wibestories.vercel.app/__clerk`).

# Deployment

- **Platform**: Vercel (production URL `wibestories.vercel.app`; legacy deploy slug `wisprstories.vercel.app`, code/filenames stay `wisprstories`)
- **Deploy**: `vercel --prod` from project root. **Deploy-blocker (2026-08-09)**: if a local `vercel build` ever fails with `spawn cmd.exe ENOENT`, it leaves a stale `.vercel/output/` (Build Output API v3 config) that later `vercel --prod` deploys instead of the real source — symptoms: tiny upload size (~27KB), old routes served, `deploy_failed: fetch failed`. Fix: `Remove-Item -Recurse -Force .vercel\output` (and `.vercel\cache`) before deploying. Never run `vercel build` locally (it cannot work on Windows via cmd.exe spawn) — just deploy directly.
- **Local dev**: `vercel dev` or open `wisprstories.html` directly in browser. **Requires Node 24.x** — the project pins `nodeVersion: 24.x` and `vercel dev`'s edge-runtime sandbox crashes on Node 25 (`Buffer is not defined` during edge function instantiation, which also kills the whole dev server via a CLI `taskkill` bug on Windows). Portable Node 24 used in past sessions: `C:\Users\srini\AppData\Local\Temp\opencode ode24 ode.exe` running `...\AppData\Roaming pm ode_modules\vercel\dist\vc.js dev`. **After adding NEW static files (e.g. demo videos), restart `vercel dev`** — it indexes static files at server start and returns 404 for files added later (2026-07-31: `animo-wheel-spin-bottom-720p.webm` 404'd until restart). Recommended permanent fix: install Node 24 LTS.
- **Docs local dev**: `npx vitepress dev docs-src` (starts VitePress dev server on port 5173)
- **Docs build**: `npx vitepress build docs-src` (outputs to `docs-src/.vitepress/dist/`)

# Testing

Main app testing is manual. See `docs/product-guide/wibe-stories.md` for test scenarios.

Runnable verification scripts (Node.js 18+, Windows-friendly):
- `node scripts/stress-test-99-cap.mjs` (add `--base=https://...` to test prod)
- `node scripts/verify-cron-cleanup.mjs` (requires `CRON_SECRET` env var)
- `node scripts/verify-rewrite-status.mjs`
- `node scripts/migrate-pro-emails.mjs` (requires `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` env vars)

Remotion demo testing: see `remotion-demo/` for test and render commands.

# Documents referenced every session

- [ ] `docs-src/` — VitePress documentation source (7 pages, Markdown frontmatter, custom theme)
- [ ] `docs-src/.vitepress/config.js` — VitePress configuration (cleanUrls, logo, nav, sidebar, base path, fonts)
- [ ] `docs-src/.vitepress/theme/custom.css` — VitePress custom theme (fonts, colors, links)
- [ ] `docs-internal/DEVELOPER.md` — developer guide (architecture, code structure, deployment)
- [ ] `docs-internal/API.md` — API reference (29 endpoints, error codes, webhooks)
- [ ] `docs-internal/limitations.md` — internal truth counterpart to public limitations
- [ ] `docs-internal/admin-setup.md` — admin activation setup
- [ ] `docs-internal/wispr_flow_company_intelligence.md` — Flow company background
- [ ] `docs-internal/wispr_flow_research.md` — Flow research notes
- [ ] `docs-internal/wispr_flow_improvement_areas.md` — Flow improvement areas
- [ ] `docs-internal/interview-quick-reference.md` — interview prep source of truth
- [ ] `docs-internal/daily-capacity-system.md` — daily capacity system
- [ ] `docs-internal/monitoring-reliability-architecture.md` — 4-layer monitoring architecture (Sentry, health, Better Stack, QStash)
- [ ] `docs-internal/Tanay-linkedin-posts.md` — Wispr Flow CEO thinking (mandatory per Tanay lens rule)
- [ ] Notion "WIBE_STORIES frontlogs" — pending items, decisions, services, user updates (source of truth; migrated from local PENDING.md, DECISIONS.md, USEFUL_SERVICES.md, C_WS-updates.md)
- [ ] `frontlogs/EMAILS.md` — email types catalog (12 types, ALL Resend since 2026-08-17 — Loops retired; classification rule + addresses; integration status)
- [ ] `frontlogs/CURRENT.md` — current phase, task, next task

## Project overview

- Tagline: "Turn your voice into shareable cards, in your language, in seconds."
- Hero subtitle: "Record with the mic or dictate with Wispr Flow. Style and share with love."
- Multi-file vanilla HTML/CSS/JS voice-to-card app. No build step for the app itself. Open `wisprstories.html` to run.
- VitePress documentation in `docs-src/` has a build step (`npx vitepress build docs-src`) that runs on Vercel deploy. Docs served at `/docs/`.
- Serverless API routes via Vercel. `package.json` must exist for dependency install.
- `remotion-demo/` — **Marketing demo video project** (Remotion/React). Created by a separate AI agent. **Do NOT delete** — it's a marketing asset, not part of the live app. Gitignored + .vercelignored.

## Key files

- `docs-src/` — VitePress documentation source (7 pages, Markdown + YAML frontmatter, VitePress theme)
- `docs-src/.vitepress/config.js` — VitePress config (nav, sidebar, base: /docs/, cleanUrls, logo, fonts)
- `docs-src/.vitepress/theme/custom.css` — custom theme (DM Sans, heading/body/link colors, light/dark + table styles)
- `docs-src/.vitepress/public/assets/` — logo images (logo-light.png, logo-dark.png) for VitePress nav
- `docs-src/index.md` — VitePress home page
- `docs-src/product-guide/wibe-stories.md` — full product documentation (vision, architecture, features, roadmap)
- `docs-src/product-guide/trust-center.md` — services used (Deepgram, OpenRouter, Vercel, etc.)
- `docs-src/reference/acknowledgements.md` — known issues (WebM audio, Firefox, translations)
- `docs-src/reference/limitations.md` — product limitations (caps, expiry, moderation)
- `docs-src/reference/roadmap.md` — Product roadmap placeholder
- `docs-src/updates/changelog.md` — version history
- `docs-src/legal/` — 4 legal pages (License, Terms, Privacy, Refund Policy) in VitePress Markdown, path-keyed sidebar under `/legal/`
- `docs-internal/DEVELOPER.md` — developer guide (getting started, code structure, deployment)
- `docs-internal/API.md` — API reference (29 endpoints, error codes, webhooks)
- `docs-internal/limitations.md` — internal truth counterpart to public limitations
- `docs-internal/agsync-archive.md` — archived agsync session notes (verbatim; the AGENTS.md agsync block keeps only the last ~4)
- `api/voice.js` — audio upload endpoint (called after PNG upload with `X-Short-Id`; stores WebM only — the Apple-compatible M4A is produced lazily by `api/voice/m4a/[id].js`, never at upload time; rate limit 500/day per IP, key `wispr:voice-ip:<ip>:<date>`, admin bypass via `x-admin-secret`)
- `api/voice/m4a/[id].js` — lazy Apple-compatible transcode endpoint (v0.11.30.0): HEAD returns 200 iff m4a OR webm exists (never redirects/transcodes — `/c/:id` uses this for `hasVoice`); GET 302-redirects to the Blob m4a if cached, else fetches the WebM, transcodes with ffmpeg (`-vn -c:a aac -b:a 96k -movflags +faststart`, 25 s timeout), stores `voice/<id>.m4a` (contentType `audio/mp4`, 5-day cache), then 302s. 302s carry `Cache-Control: no-store`. Transcode failure → 404 (players fall back to WebM; next request retries)
- `api/rewrite-confirm.js` — tone rewrite commit endpoint (Redis INCR on per-tone counter)
- `api/rewrite-status.js` — read-only GET endpoint for per-tone counts from Redis
- `api/stt.js` — STT via Deepgram Nova-3 + Whisper/OpenRouter, Edge runtime
- `api/clerk-proxy.js` — Clerk proxy mode edge function: forwards `/__clerk/:path*` (via `?clerkPath=` query param, top-level — NOT a subdirectory catch-all, which only matches single-segment paths on Vercel) to `frontend-api.clerk.dev` with `Clerk-Proxy-Url` + `Clerk-Secret-Key` headers; `redirect:'manual'` passes Clerk's 307 version-redirects back so the browser re-enters the proxy at the exact-version path
- `api/resend-key.js` — automated Pro key recovery via email (resends key by email lookup)
- `wisprstories.html` — main HTML (includes the Welcome landing page overlay markup, `lp-*` classes, `#onboardingOverlay` / `#onboardingGotIt`)
- `wisprstories.js` — app logic (STT, recording, card creation, share, tones, i18n; `showOnboarding`/`hideOnboarding` first-visit landing page)
- `pricing.html` — standalone pricing page (modal Plans content + nav/footer chrome; Pro banner via `global/pricing-page.js`; clean URL `/pricing` via vercel.json)
- `404.html` — custom 404 page (full-viewport video background, glitch effect ON TOP via mix-blend-mode, 3D tilt, text scramble on CTA, magnetic pull on logo, grain overlay; context-aware: shows blog variant for /blog/ paths — logo `/assets/brand/BLOG-LABEL.png`, CTA `/blog/`, "Go to blog home"; untracked in git). Vercel serves it as the custom error page for ANY unmatched route, so all missing `/blog/*` URLs (catch-all rewrite misses) already render the blog variant; vercel.json `/blog/404` rewrite explicitly points at `/404.html` too (was blog dist's VitePress-default 404.html until 2026-08-19). SEO: noindex, OG tags, canonical, favicon; video protection: no download/right-click/drag
- `global/pricing-page.js` — pricing page behaviour (theme, back-to-top, Pro banner, tier picker, CTA wave)
- `global/fonts.js` — script font mapping
- `global/demo.js` — demo animation (disabled, preserved for restoration)
- `global/capacity-check.js` — daily capacity check (99-user cap), admin/pro key headers
- `global/styles/` — CSS modules (17 files)
- `global/pricing-modal.js` — pricing comparison modal (triggered from footer menu). v0.11.38.0 fix batch (mirrors the pricing-page fixes): overlay `mouseenter`/`mouseleave` CTA-wave handlers guard `e.target instanceof Element ? e.target.closest(".pricing-cta") : null` (target can be `document` when the pointer leaves the window → `.closest` TypeError); `slideTo()` re-anchors the toggle slider instantly after the 120 ms cost-text swap (`transition: none` + `void offsetWidth` + restore); root cause in `overlays.css` — `.pricing-pro-cost` now `width: 120px` (keeps `flex-shrink: 0; min-width: 90px`) so the "🎖 Best Value" detail can never expand the cost block and shift the track; `main.css` overlays import bumped to `?v=20260818-v0.11.38.0` (cache-bust). CDP-verified: 0 px drift on pricing page + modal, crash guard no-ops, zero window errors. Note: the modal's Plans panel + Plans tab are `display: none` (show() always opens the Gift tab), so the picker/CTAs are unreachable today — the drift + crash fixes are latent-safety; the cost-width CSS is live wherever the picker renders. **2026-08-19: reconstructed after accidental comment-removal edit corrupted file (467→323 lines, ~144 lines deleted including handleKey/handleEmail/_disposableDomains/key panel/key+email listeners/whitelists/hidden Plans tab). Restored from HEAD verbatim while preserving gift-code work (handleGiftCode, gift tab/panel, instanceof guards, slideTo fix). Fix probe passes: 0px drift, crash guard works, wave animation restored, zero window errors.**
- `global/vault.js` — Wibe Vault overlay (browse, select, delete, audio playback, localStorage persistence)
- `global/styles/vault.css` — Wibe Vault styles (CSS Grid layout, tile animations, audio badge, dark mode)
- `global/styles/pricing.css` — pricing page styles (page chrome, golden Pro banner, CTA wave keyframes)
- `global/styles/layout.css` — layout styles (footer menu divider)
- `assets/i18n/` — 11 UI locales + `en.json`. **Do not regenerate deleted locales.**
- `assets/i18n/i18n.js` — i18n loader with `data-i18n`, `getI18nSync()`
- `assets/brand/` — brand assets (logos in 4 variants, favicons, OG template, YGL footer logo, ghost images, demo GIF)
- `assets/occasions/` — 62 occasion images (.png)
- `api/health.js` — health endpoint (Redis ping, Neon SELECT 1, API key presence checks). v0.11.30.0 semantics: 503 ONLY when both Redis AND Neon are in `error` state (missing env vars → `unknown`, never contributes to 503); single-service blips return 200 with `degraded`; 3 s per-check timeout
- `api/` — Vercel API routes
- `lib/og-render.js` — OG image generator (composites card onto brand/WS-OG-Image.png template; moved from api/lib in v0.11.30.0 so it isn't deployed as a stray serverless function)
- `lib/occasion-email.js` — occasion email template builder (39 global occasions, Resend transactional API; movable-festival date table inline, `api/lib/occasion-dates.json` was legacy and is gone — moved from api/lib in v0.11.30.0). Template: dark `#111111` hero with the ImageKit illustration as `background-image` (`?tr=w-1024`, `background-size:300px auto`) + VML `v:fill type="frame"` for Outlook (`v:rect` 600×400, `v:textbox` around the card); a **200px knob spacer** (`line-height:170px`; comment: arch ~65px deep, 200 keeps the arch fully inside the illustration, up to 215 safe — smaller = card higher = more illustration shows through the arch) so the **arched card** floats over the illustration; the card is a **user-designed PNG** (`CARD_URL` = `IMAGEKIT_BASE` + `/occasions/email-card.png`, 480×330, transparent hollow arch cut into the top-center) applied via `background=` + `background-image` on the td (`background-size:480px 330px`), **no bgcolor/border/radius** (a cream bg would fill the arch) (v0.11.32.0; applies to all 39 emails); the card holds an 88px arch-clear spacer, the "OCCASION" tag pill (1px `#1a1a1a` border, DM Sans 12px bold — v0.11.32.0 dropped the "NEW" prefix), an 8px gap, the headline image (`HEADLINE_BASE` + `/headlines/{id}.png` + `HEADLINE_TRANSFORM = 'tr=w-960&v=3'`, ImageKit PNG rendered in Dela Gothic One from the name as stored — title case, 960×240 source shown 480×120, `alt` = occasion name; bump `v=` whenever headline PNGs are re-uploaded so the CDN cache never serves stale renders; eight names render on two explicit lines via `HEADLINE_LINES` in `scripts/generate-occasion-headlines.mjs` (six 3-token names — new-year, lunar-new-year, st-patricks-day, april-fools-day, dragon-boat-festival, new-years-eve — plus mother-language-day 'International Mother'/'Language Day' and world-voice-day 'World'/'Voice Day'); other 4+-token names auto-wrap), then the flags-only row and a 16px bottom spacer; CTA "CREATE YOUR CARD" sits BELOW the card (amber `#f5a623`, 4px radius, Arial bold 15px, 16px 60px padding); then dark see-plans strip, DM Sans cream body at 16px/1.5 (greeting italic line, `occasions-mail.gif` sample animation 440×330), **flags row is flags-only** (v0.11.32.0: no country names, no `&middot;`, no parenthetical, no 6-flag cap — every flag in the copy data renders; caption "These countries celebrate this occasion"; **Lunar New Year carries 12 flags `cn, vn, kr, sg, my, id, ph, mm, th, bn, tw, hk`**; copy data is GENERATED from `frontlogs/emails/occasion-copy.md` via `scripts/gen-occasion-copy.mjs` — edit the .md, never the .js), dark footer (HQ, reason, speak·scribe·share, per-recipient unsubscribe link `/unsubscribe?e=<urlencoded-base64>`). The headline-PNG pipeline is **live** — the template references `/headlines/{id}.png`, and `scripts/verify-occasion-email.mjs` (84 checks incl. XSS/fallback/copy-integrity) asserts the arched design. Static sample: `frontlogs/emails/Occasion-Email-NEW.html` (independence-day, "Priya"); preview tool: `frontlogs/ups_pres/occasion-email-preview.html` (mirrors the lib template, all 39 occasions; embeds `CARD_URL`).
- `api/cron/send-occasion-emails.js` — daily cron (02:00 UTC = 07:30 India time) that matches date to occasion and emails Pro + subscriber users
- `api/cron/vault-chain.js` — daily cron (04:00 UTC; vercel.json entry added 2026-08-16) — the **Vault Chain** T+30/T+60/T+90 email sequence (Resend, transactional, dormant until `VAULT_CHAIN_ENABLED=1`). Audience = `wispr:pro-emails` set → `KEYS.emailLookup(email)` → `KEYS.upgradeKey(key)` JSON `{tier, expiresAt, revoked, cancelled}` (monthly keys have NO `expiresAt` → never in chain; cancelled annual keys keep `expiresAt` → still chained). Stages (lower-bound day checks survive cron downtime): T+30 (>=30d, vault has cards → warning), T+60 (>=60d, cards → lastCall + set `wispr:vaultchain:hadcards:<email>`), T+90 (>=90d + hadcards flag → delete vault blobs `cards/`+`og/`+`voice/`+`meta/` prefixes via `@vercel/blob` list/del batches of 50 + `DELETE FROM vault_cards WHERE pro_key=...` via `getNeon()` → farewell). Dedupe keys `wispr:vaultchain:{30|60|90}:<email>` (10y TTL); CONCURRENCY 5; CRON_SECRET Bearer auth; templates in `lib/vault-chain-email.js`; Sentry on error.
- `api/cron/milestones.js` — daily cron (05:00 UTC; vercel.json entry added 2026-08-16) — **Milestone Celebration** marketing email via Resend (dormant until `MARKETING_ENABLED=1`; rewritten 2026-08-17 — was a Loops `events/send`, now calls `sendMilestoneEmail` from `lib/milestone-email.js`). Thresholds 10/25/50/100/250/500; per key = highest threshold ≤ `COUNT(*)` in `vault_cards`; skips revoked/cancelled/expired keys; dedupe `wispr:milestone:last:<key>` = JSON `{threshold, ts}` (skip if last.threshold >= milestone or < 90d cooldown); name personalization via `KEYS.subscriberName`; CONCURRENCY 5; CRON_SECRET Bearer auth; Sentry on error.
- `lib/vault-chain-email.js` — Vault Chain email templates (3: `warning`, `lastCall`, `farewell`) + `sendVaultChainEmail(resendApiKey, {stage, toEmail, name, count, endDate})` — cream `#fff8eb` body style, amber `#f5a623` CTA, dark footer, "Contact us"/"Help us improve" links (decision #8), NO unsubscribe link (transactional by design); name fallback "there"; 8s AbortController; uses `KEYS.subscriberName` for personalization.
- `lib/marketing-email.js` — shared marketing email shell for ALL Resend marketing templates (milestone, founder letter, expiry) + `sendMarketingEmail(resendApiKey, {...})`: cream `#fff8eb` body, amber `#f5a623` CTA, dark footer with "Contact us" (tally.so/r/obaD1M) + "Help us improve" (tally.so/r/jaqlJ6) links (decision #8), **one-click unsubscribe footer** → `/unsubscribe?e=<urlencoded-base64>` (marketing only — transactional vault chain intentionally has none); `ctaButton`, `unsubHref`, `htmlEscape`, 8s AbortController; sends via plain `resend.emails.send` (Broadcasts = documented future bulk path).
- `lib/milestone-email.js` — Milestone Celebration template + `sendMilestoneEmail(resendApiKey, {toEmail, name, threshold, count})` (replaces the retired Loops milestone workflow, 2026-08-17)
- `lib/founder-letter-email.js` — Founder Letter template + `sendFounderLetter(resendApiKey, {toEmail, name})`; `FOUNDER_SUBJECT = 'Why send a borrowed wish?'`; copy verbatim from `frontlogs/emails/founder-letter.md` (no em dashes per user rule; replaces the retired Loops founder-letter workflow)
- `lib/expiry-email.js` — Pro expiry sequence templates + `sendExpiryEmail(resendApiKey, {stage, toEmail, name, expiryDate})`; stages `t-7` / `t-1` / `t+7`
- `api/cron/expiry-emails.js` — daily cron (06:00 UTC; vercel.json entry added 2026-08-17) — **Pro expiry reminders** (Resend, marketing, dormant until `MARKETING_ENABLED=1`). Audience = annual Pro keys only (`KEYS.upgradeKey` must have `expiresAt`); stages (lower-bound day checks survive cron downtime): T-7 (`daysLeft` 1..7 → renewal reminder), T-1 (`daysLeft <= 1` → last day call), T+7 (`daysGone` 1..7 → post-expiry rejoin invite). Dedupe keys `wispr:expiry:{t-7|t-1|t+7}:<email>` (10y TTL); name via `KEYS.subscriberName`; CONCURRENCY 5; CRON_SECRET Bearer auth; Sentry on error.
- `api/send-founder-letter.js` — manual Founder Letter trigger (POST, Edge): requires `x-admin-secret` === `ADMIN_API_SECRET`; `{email}` body = one test send (no dedupe); no body = audience `wispr:email-subscribers` set with monthly dedupe `wispr:founder:YYYY-MM` (`KEYS.founderSent` helper, 10y TTL, set ONLY on a zero-error run so retries after partial failure still send); CONCURRENCY 5; Sentry on error.
- `api/webhooks/resend.js` — Resend tracking webhook (POST, Node runtime): **Svix-standard signature verification** — headers `svix-id`/`svix-timestamp`/`svix-signature` (`v1,` + base64 HMAC-SHA256 over `{id}.{timestamp}.{rawBody}`, secret = `RESEND_WEBHOOK_SECRET` with `whsec_` prefix stripped + base64-decoded, 300s replay window, `timingSafeEqual`); stores whitelisted events (`email.sent`, `email.delivered`, `email.delivery_delayed`, `email.complained`, `email.bounced`, `email.opened`, `email.clicked`) into Neon `email_events` (self-healing `CREATE TABLE IF NOT EXISTS` — `id BIGSERIAL PK, message_id, email, event, url DEFAULT '', created_at` — + indexes `idx_email_events_message`, `idx_email_events_event`; clicked URL from `data.url` or `data.payload.link`); 400 on bad signature, 200 on non-whitelisted event.
- `frontlogs/emails/founder-letter.md` — approved Founder Letter copy (no em dashes per user rule; canonical source — `lib/founder-letter-email.js` renders it verbatim)
- `frontlogs/emails/loops-built.json` — record of the retired Loops asset IDs (components/workflows/groups) — cleanup reference only for closing the Loops account (2026-08-17)
- `api/subscribe-occasion.js` — email subscription endpoint (validates + stores in Redis; Redis is the sole source of truth — the Loops dual-write from v0.11.34.1 was removed 2026-08-17)
- `api/unsubscribe-occasion.js` — occasion email unsubscribe endpoint (Edge; friendly path `/unsubscribe` via vercel.json rewrite; **GET** = one-click unsubscribe — removes from `wispr:pro-emails` + `wispr:email-subscribers`, sets Undo-gate marker `wispr:unsub-gate:<email>` (EX 3600) only if a member was actually removed; **POST** = undo — **gate-checked via `GETDEL`** (missing gate → 400 "already been processed", blocks phantom resubscribes from forwarded links), re-adds to both sets; token lowercased after decode; subscriber name key intentionally kept so undo restores exact state; HTML page with 💔 favicon + icon; errors 400/500; Loops sync from v0.11.34.1 removed 2026-08-17). The email template links `https://wibestories.vercel.app/unsubscribe?e=<urlencoded-base64>` (`encodeURIComponent` — raw base64 can contain `+`/`/` which `URLSearchParams` would mangle)
- `api/test-send-occasion.js` — dev-only occasion email test endpoint; v0.11.30.0: returns 401 unless `x-admin-secret` matches `ADMIN_API_SECRET`
- `frontlogs/` — internal planning files (git-ignored): `EMAILS.md`, `CURRENT.md`, `ups_pres/` (email preview HTMLs). PENDING.md, DECISIONS.md, USEFUL_SERVICES.md, and C_WS-updates.md migrated to Notion (see Frontlogs section below)
- `lib/redis.js` — Upstash Redis client (includes `unsubGate(email)` key helper for the unsubscribe Undo gate; `founderSent(month)` = `wispr:founder:YYYY-MM` and `expirySent(stage, email)` = `wispr:expiry:{t-7|t-1|t+7}:<email>` dedupe helpers, added 2026-08-17)
- `lib/allowed-emails.js` — email domain allowlist (Gmail, Outlook, Yahoo, Proton, iCloud, Tuta + regional variants)
- `lib/lang-stats-redis.js` — separate Redis client for lang usage stats
- `lib/chart.umd.min.js` — Chart.js bundle
- `api/track-usage.js` — card creation usage tracking
- `api/lang-stats.js` — language counters GET
- `language-stats.html` — Language Stats page with Chart.js + data table
- `docs-internal/existing-redis.md` — Upstash Redis architecture
- `docs-internal/daily-capacity-system.md` — 99-user capacity system
- `docs-internal/language-stats-page.md` — stats page architecture
- `global/styles/language-stats.css` — stats page CSS
- `global/language-stats.js` — stats page JS
- `about.html` — About page
- `features.html` — Features page with 5 capability sections (Speak, Write, Rewrite, Design, Share)
- `global/styles/about.css` — About page CSS
- `global/styles/features.css` — Features page CSS
- `global/about.js` — About page JS
- `global/features.js` — Features page JS
- `vercel.json` — deployment config, CSP security headers, .html redirect. `/card` rewrites to `/wisprstories.html` directly (v0.11.30.0: `api/card.js` deleted; a rewrite to `/` alone 404s — Vercel doesn't chain rewrite-to-root, so point at the real file)
- `opencode.json` — project-scoped OpenCode config (registers the OpenSEO remote MCP for this project only; see the MCP sections below). No secrets — safe to commit. (The Loops MCP entry was added 2026-08-15 and removed 2026-08-16 — see the Loops section below.)
- `sitemap.xml` — 17 URLs: 5 app pages + blog home + 11 docs pages. Blog locale variants, category pages, and blog posts are deliberately omitted (see the comment inside the file; posts return when real articles ship). Update when new pages ship.
- `.vercelignore` — excludes remotion-demo/ from deployments
- `sw.js` — service worker; NEVER intercepts cross-origin requests (internal fetch runs under page connect-src — interception caused CSP blocks/503s for Blob, R2, then Clerk bundles; structural fix v20: same-origin only)
- `global/footer-menu.js` — footer menu rendering, i18n, reorder, occasion email subscription popup
- `global/occasions/` — occasion triggers, date-occasions, country mapping
- `remotion-demo/` — **Marketing demo video project** (Remotion/React). Do NOT delete.
- `scripts/stress-test-99-cap.mjs` — load test for 99-user cap
- `scripts/verify-cron-cleanup.mjs` — cleanup auth test
- `scripts/verify-rewrite-status.mjs` — rewrite-status test
- `scripts/migrate-pro-emails.mjs` — one-time Pro email backfill into Redis set for occasion campaigns
- `docs-internal/every-design-decision-explained.md` — architecture Q&A
- `docs-internal/model-comparison.md` — model latency/pricing comparison for rewrite chains
- `docs-internal/PRICING.md` — pricing documentation (cost breakdown, profitability math, tiers, key naming, purchase flow, expiry timeline)
- `assets/card-bgs/spiral-overlay.webp` — grayscale spiral overlay for custom color background (used with `background-blend-mode: overlay`)
- `internal-logs/ilogs-ws.md` — acknowledged logs source-of-truth (git-ignored)
- `internal-logs/observer.js` — keyboard shortcut handler for beacon redirect
- `api/beacon.js` — redirect handler (reads `WS_EP` env var)
- `api/download/[id].js` — download proxy (serves Blob files with `Content-Disposition: attachment`)
- `lib/neon.js` — Neon Postgres connection singleton (edge-compatible HTTP query via `@neondatabase/serverless`)
- `lib/sentry.js` — Sentry init for Edge runtime API routes (`@sentry/vercel-edge`). **Lazy-loads the SDK via fire-and-forget dynamic import with a no-op fallback** — the SDK's ESM build imports `node:async_hooks`/`node:events` which the local `vercel dev` sandbox can't polyfill (would crash edge routes with `Buffer is not defined`); production loads the SDK normally and behaves as before (only a short async-init window drops early errors)
- `lib/sentry-node.js` — Sentry init for Node.js runtime API routes (`@sentry/node`)
- `api/vault/list.js` — GET vault cards for a Pro key (Neon query, camelCase response); includes `last_accessed_at` column auto-added via `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`; auto-restore adoption logic for returning Pro users; sort toggle + search/filter UI; `last_accessed_at` touched on vault open
- `api/vault/save.js` — POST save a vault card (validates Pro key, enforces 50-card limit, Neon insert); `last_accessed_at` DEFAULT NOW() on insert; `image_url` column auto-added
- `api/vault/delete.js` — POST delete vault cards by client_id array (scoped to Pro key); `last_accessed_at` column added via ALTER
- `api/vault/migrate.js` — POST batch-migrate localStorage cards to server (for new Pro users upgrading); `last_accessed_at` column added via ALTER + index `idx_vault_cards_last_access`
- `api/vault/rename.js` — POST rename a vault card (validates Pro key, 60-char name cap, updates `name`, `updated_at`, `last_accessed_at`); same auth/CREATE/ALTER pattern as siblings

## Wibe Vault system

- `global/vault.js` — full-screen overlay invoked from footer menu or creation flow
- `global/styles/vault.css` — CSS Grid layout (`auto-fill, minmax(250px, 1fr)`), `tile-in` staggered animation, audio badge with play toggle, Select All/Delete flow, toast Undo button
- **Storage**: Neon Postgres (`vault_cards` table) for Pro users, localStorage (`wsVaultCards`) for free users. `saveCardToVault()` in `wisprstories.js` saves automatically after upload for Pro users. `last_accessed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()` column tracks when each card was last accessed; set on vault open via `api/vault/list.js` and automatically on card rename/delete.

- **Lifecycle**: Open → load from Neon API (Pro) or localStorage (free) → render grid → select/delete/download (API for Pro, localStorage for free) → persist on mutation. `last_accessed_at` updated on vault open, rename, and delete.

- **Pro auto-save**: On Create Card click (btnC), Pro users' cards are automatically uploaded in the background and saved to vault with a 6s Undo toast. Share button (btnS) never saves to vault — it reuses btnC's `_shareBlob` and `_shortId` via the `_vaultAutoSaved` shortcut path, or uploads fresh for the share flow without vault save. The auto-save IIFE is stored as `window._vaultAutoSavePromise`; btnS awaits it (with catch) before the fast-path check so a pending auto-save can't race the share upload

- **Vault UI enhancements**: 
  - **Loading skeletons** (`_loading` flag): shimmer placeholders shown while cards load from server, reduced-motion safe.
  - **Sort toggle**: newest/oldest toggle persisted in `localStorage["wsVaultSort"]`; header button updates label/icon; sorted copy never mutates underlying cards.
  - **Search/filter**: live case-insensitive filter on card name/text; clear button; Esc clears; "No cards match" state when query returns no results; Select All operates on filtered set; counter always shows total saved.
  - **Rename card**: pencil icon in card-view header → inline contenteditable name (Enter commits, Esc cancels, blur commits); POST `/api/vault/rename` on success; toast + revert on failure.
  - **Error state**: `_loadFailed` flag; set on HTTP error/catch; cleared when usable data shown; `.vault-error` markup with `#vault-retry-btn` → `loadCards().then(render)`.

- **API**: `api/vault/list.js` (GET), `api/vault/save.js` (POST), `api/vault/delete.js` (POST), `api/vault/migrate.js` (POST), `api/vault/rename.js` (POST) — all guarded by Pro key validation
- **DB**: `lib/neon.js` connection singleton, `vault_cards` table with indexes on `pro_key` and unique index on `(client_id, pro_key)`. `last_accessed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()` column now in DB (auto-added via `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` in all 5 vault API endpoints). `image_url TEXT` column now in DB (auto-added via ALTER in all 5 endpoints). Card images now display in vault tiles and card view. Unique index on `(client_id, pro_key)` enforced.
- **Auto-create table**: All 4 vault API endpoints (`save.js`, `list.js`, `delete.js`, `migrate.js`) include `CREATE TABLE IF NOT EXISTS vault_cards (...)` on every request — self-healing if the table is missing in Neon. Schema matches `scripts/setup-neon-table.mjs` plus `image_url TEXT NOT NULL DEFAULT ''`. Each endpoint also runs `ALTER TABLE vault_cards ADD COLUMN IF NOT EXISTS image_url` for existing tables. Indices not auto-created.

## Card metadata sidecar system

- `api/upload.js` stores `meta/<shortId>.json` alongside card images (`{ text, name, tone, p, r, theme, pro }`). v0.11.30.0: also rate-limited 200/day per IP, key `wispr:upload-ip:<ip>:<date>`, admin bypass via `x-admin-secret`.
- `api/c/[id].js` fetches metadata to personalize landing page. Old cards fall back gracefully.
- `meta/` cleaned up by `api/cleanup.js` (7 days for free, 14 days for Pro).

## Welcome landing page system

- `wisprstories.html` — the landing is the **first-paint page** for new visitors (not an overlay): `#onboardingOverlay` is visible by default — a centered **95vw × 95vh card** (radius 14px, soft shadow, solid `#1a1a1a` card background — **always dark by design, the landing never renders in light mode**) floating over a `#fff8eb26` 15%-cream-wash backdrop (no backdrop blur) — **on mobile (≤720px) the backdrop is opaque `#2A2A2A` instead, fully hiding the app behind** — so on desktop the real app shows through blurred/dimmed **around the card's edges** — the sides of the background must stay visible (user-specified size, do not make the banner full-bleed). Contains `lp-nav` (brand logo — white-on-transparent variant + Features/About/Pricing links + `.lp-nav-auth` Sign in/Sign up buttons), `lp-hero`, `lp-benefits` (4 badges), `lp-cta` (`#onboardingGotIt`), `lp-video-wrap` (`#lpVideo` WebM demo, **src fully JS-managed** — `mob-sc-1.webm` (1080×1350 portrait) ≤720px, `wheel-showcase-1-1080p.webm` (1920×1080) ≥721px - hosted on Cloudflare R2 (see Video-Base bullet); **clicking toggles shot 1 ↔ 2** (`_lpVideoShot`); a `matchMedia("(max-width: 720px)")` change listener swaps the pair instantly on breakpoint crossing, same shot index (`lpIsMobile`/`lpCurrentFile`/`lpSetVideoFile` — **no `<source media>`**: for `<video>` those are evaluated only at selection time and never react to resize, see the agsync 2026-08-01 note); playback starts only for first-timers — the `ws-app-return` guard inside `lpSetVideoFile` means returners never fetch the video), a `.lp-hint-pill` ("Tap to switch view", always visible while the landing is up - never fades on click; gone for good once the landing exits (it never reopens) -, bottom-center above the footer — `bottom: 34px`, centered, `#ffffeb33`, no background/border), and a `.lp-footer` ("Wibe Stories © 2026 YGLabs", absolute at the card/page bottom — 14px bottom padding, 11px, `font-weight: 100`, `#ffffeb55` — floating over the video's transparent bottom strip). No close button, no backdrop dismissal — CTA is the only entry. Nav links use `/features`, `/about`, `/pricing` (routes to the standalone pricing page; Sign in/Sign up no-op placeholders). **English-only by design** — static markup, zero i18n hooks. The landing markup lives AFTER the app block (before end-of-body scripts) so `#appRoot` can wrap the whole app in one element.
- **`#appRoot` backdrop + lock**: the entire app is wrapped in `<div id="appRoot">`. While the landing is up: `position: fixed; inset: 0; overflow: hidden; transform: scale(1.04); filter: blur(8px) brightness(0.55) saturate(0.8); pointer-events: none` + `inert`/`aria-hidden` (JS-added by `focusLanding()` for **first-timers only** — never bake them into the HTML or returners would be permanently locked). `html.ws-app-return` restores normal flow.
- **Pre-paint returner flip**: the head script (wisprstories.html ~line 95, next to the theme init) adds `html.ws-app-return` when `localStorage["wsOnboardingSeen"]` is set — before first paint. Returners: landing `display: none`, appRoot normal → app as first frame. First-timers: landing as first frame. No flash either way.
- `wisprstories.js` (~line 3470) — `focusLanding()` locks the app (inert/aria-hidden; the CTA auto-focus was removed — see the v0.11.28.1 ring fix below); `enterApp()` (guarded by `_enteringApp`) adds `ws-entering-app` → landing fades 0.35s while app unblurs/zooms 0.55s → after 600ms (or immediately under `prefers-reduced-motion`): sets flag, adds `ws-app-return`, removes `inert`/`aria-hidden`, pauses `#lpVideo`, then re-checks version state (`if (_updatePending) showUpdateToast(); else checkVersion();`). `showUpdateToast()` no-ops while `ws-app-return` is absent. No reopen path — landing shows once until "Got it" is pressed, then never again.
- `global/styles/overlays.css` — landing + backdrop states: `.onboarding-overlay` (default visible, flex-centered, always `#fff8eb26` 15% cream wash on desktop — **opaque `#2A2A2A` at ≤720px**, no backdrop blur, `overscroll-behavior: none`), `.onboarding-banner` (95vw × 95vh card, radius 14px, shadow, always `#1a1a1a` — landing is always dark, no light variant, `overflow-y: auto` for short screens + `overscroll-behavior: contain` so landing scroll can never chain to the app — **do not make it full-bleed**), `#appRoot` locked backdrop, `html.ws-app-return` (returner flip), `html.ws-entering-app` (enter transition), `prefers-reduced-motion: reduce` (no animation), `.lp-*` content styles (nav/hero/benefits/CTA/video, nav links hidden under 720px, `.lp-nav-auth` flex gap 10px, `.lp-nav-signin` ghost / `.lp-nav-signup` filled pill, `.lp-cta-btn` amber `#F59E0B` + **literal `#1a1a1a` ink text** + `outline: none` (no focus ring — see v0.11.28.1), `.lp-video` fills the remaining card height (`flex: 1` wrap + `min-height: 0`, video `height: 100%`, **desktop ≥721px: `width: 100%` + `object-fit: cover` (cropped, edge-to-edge - wrap padding removed/commented out)** / **mobile ≤720px: `width: auto` + `height: auto` + `max-width/max-height: 100%` + `object-fit: contain` (portrait mob-sc video largest-fit — no crop, no letterbox), wrap padding removed (commented out - video flush edge-to-edge), hero title `clamp(34px, 8vw, 44px)`, subtitle 13px, benefit badges 10.5px/`4px 10px` — mobile hierarchy: title clearly leads, badges stay below CTA weight** — the page never scrolls, video sits at the bottom, `cursor: pointer` on the video). `.lp-footer` (absolute page-bottom footer — 14px bottom padding, 11px, `font-weight: 100`, `#ffffeb55`, `pointer-events: none` — floats over the transparent video strip) and `.lp-hint-pill` (bottom-center above the footer — `bottom: 34px`, centered, 10.5px, `#ffffeb33`, no background/border, `pointer-events: none` - always visible while the landing is up, no fade machinery (removed per user spec)). **All landing colors are hardcoded dark-mode literals (`#ffffeb` family) — the landing never depends on the theme; no `:root.dark` landing overrides exist, do not re-add them.**
- **i18n**: Landing has **zero i18n keys** (English-only, see above). `footer.help` ("How to Use") was removed from all 11 locales in v0.11.27.0 along with its consumers — do not restore. The `landing` block survives only in `en.json` (canonical English copy); the 10 non-English locales no longer have it. Other `landing.*` keys (`navDocs`/`navContact`) were also removed in v0.11.27.0 — do not restore.
- **Video hosting**: Cloudflare R2 bucket `wibe-stories-media`, subfolder `welcome-landing/`, served publicly via r2.dev. Base: `https://pub-5b5b01c9c3a14dad80d7d71e76a269b4.r2.dev/welcome-landing/`. Four files (all verified live 200 `video/webm`): `wheel-showcase-1.webm` (2560x1440, 67.6 MiB - desktop >=721px shot 1; re-uploaded 2026-08-16 restoring true transparency, HEAD 200 70,884,884 B), `wheel-showcase-2.webm` (2560x1440, 43.3 MB - desktop shot 2), `mob-sc-1.webm` (1080x1350, 13.0 MB - mobile <=720px shot 1), `mob-sc-2.webm` (1080x1350, 6.7 MB - mobile shot 2). **All four are transparent WebM (VP9, alpha_mode: 1)** - they work on both light and dark mode; transparency was verified by rendering each file in a headless browser over a red background (the old opaque `*-1080p.webm` re-encodes were deleted from R2 on 2026-08-09 - they had black backgrounds and were the cause of the "spoiled transparency" complaint; do not reintroduce opaque encodes). ffmpeg cannot extract the alpha plane from these masters ("planes not available") - never re-encode the landing videos with ffmpeg; the browser renders the alpha directly from the original files. src chosen per viewport + shot in JS (`lpIsMobile`/`lpCurrentFile`/`lpSetVideoFile` - click toggles the shot, a `matchMedia` change listener swaps the pair on breakpoint crossing and preserves `currentTime`; `prefers-reduced-motion` skips autoplay until the user clicks). Videos are git-ignored (`assets/cards/*.webm|mp4|gif`) and must never be committed. `sw.js` bypasses `.r2.dev` hostnames entirely (same pattern as the Vercel Blob bypass) so the SW never intercepts the video request - its internal fetch() would violate CSP connect-src; the browser loads the video directly under media-src.
- **R2 user-content bucket**: Second R2 bucket `wibe-user-content` (Location Automatic, Standard) created 2026-07-31 for user-generated card content (cards, OG previews, voice clips, metadata sidecars) — candidate replacement for Vercel Blob. Credentials live in `.env` (`R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`) and Vercel dashboard env vars. App code migration (replace `@vercel/blob` calls in `api/upload.js`, `api/cleanup.js`, `api/c/[id].js`, `api/download/[id].js`, `api/og/[id].js`, `api/voice.js`) is a separate pending task.

## Wibe & Wonder blog (blog-src/)

- **What**: VitePress blog publication "Wibe & Wonder" (9 locales: en/th/ko/ja/es/it/tl/tr/sv), served at `/blog/` via rewrites in `vercel.json`. Custom theme components (no stock VitePress layout). Sticky nav, masthead (in `BlogHome.vue`: "BLOG" tag pill + wordmark logo + mission sentence), category filter, month strip, featured + latest cards, pagination, related posts, article pages with AI footer CTA + article-footer stack (share, signature, disclosure, improve link, contributors), search, back-to-top. Posts currently live: 3 (see Phase 1 pipeline bullet).
- **Deploy**: `vercel --prod` from root (same as main app). Local preview: `npx vitepress dev blog-src` (port 5174) or serve `blog-src/.vitepress/dist`; a preview server on port 4177 was used for post-build verification (`http://localhost:4177/blog/`). **Vercel buildCommand**: `node scripts/fetch-content.mjs && npx vitepress build blog-src && npx pagefind --site blog-src/.vitepress/dist && node scripts/copy-fontawesome.mjs && node scripts/gen-sitemap.mjs && npx vitepress build docs-src` — fetch-content is a no-op without CONTENT_TOKEN/CONTENT_REPO (always exits 0, never fails the build); pagefind must run after the blog build (it reads dist; index lives in `dist/pagefind/` — also add `/blog/pagefind/:path*` rewrite to vercel.json, present since 2026-08-17); copy-fontawesome copies FA webfonts into dist/assets; gen-sitemap must run after the blog build + pagefind (it reads dist). Run the same chain locally before `vercel --prod` to catch failures in the deploy preview.
- **Masthead + logos (verified 2026-08-09)**: the masthead is NOT a separate component — it lives in `BlogHome.vue` as `<header class="ws-masthead">` (`.ws-masthead-tag` "BLOG" pill + `.ws-masthead-logo` img + `.ws-masthead-sub` = `t('mission')`), identical static markup for every locale, no video variants. `BLOG-LOGO.png` (4500×843, "BLOG" wordmark, 5 bar-chart dots) sits in `blog-src/.vitepress/public/` and is also copied to repo `assets/brand/` (untracked). The nav logo (`SiteNav.vue`, `logoLight = withBase('/WS-HORI-DARK.png')`) uses `WS-HORI-DARK.png` from the blog's own `public/` for ALL locales — the old repo-root sibling reference (file never existed at root, broken in built output) is gone. Phase 2 plans per-locale masthead videos replacing the static logo.
- **Design system (2026-08-05 restyle, user-specified, keep)**: body background `--cream: #fffff6`; **zero pill shapes anywhere** (no `border-radius: 999px` remains — all converted to 4px or flat); category filters and language links are **muted text links** (`.ws-pill`: `--ink3`, 14.5px/500, hover → `--ink`, active = `--ink`/700) with no background/border; month strip buttons flat (`ws-month-on` = dark/bold, no bg/border/radius); pagination/CTA/search/back-top buttons 4px radius flat. **CTA brand accent (2026-08-15)**: `.ws-nav-cta` (desktop nav) and `.ws-nav-panel-cta` (mobile panel) are the ONLY amber elements — `#f59e0b` bg + `#1a1a1a` ink text (app brand, 4px flat, hover `#e59500`); the panel's Search go-button stays ink-dark so the CTA carries the conversion weight. **Search magnifier (2026-08-15)**: inline SVG inside the input (`.ws-search-icon`, absolute `left:.75rem`, `--ink3`, `pointer-events:none`, darkens to `--ink` via `:focus-within`); input `padding-left: 2.25rem`; no FA dependency (SVG matches the LanguagePills chevron pattern). **Mobile nav panel (2026-08-15 final)**: panel is full-page `height:100dvh` + `overflow-y:auto` + `overscroll-behavior:contain`; body locked while open via `html.ws-locked{overflow:hidden}` toggled by SiteNav watch; field order = Search → Categories → Language → Create a card (intent → browse → preference → conversion); Categories = 2-col grid of bordered 4px block buttons (`.ws-nav-panel-cat`); the language dropdown inside the panel is `position:fixed` to the SCREEN (left/right `var(--gutter)`, top + max-height set by JS from `getBoundingClientRect()` in LanguagePills — it floats over the page, contributes nothing to panel scroll, so opening it never pushes panel content; desktop dropdown stays absolute below the button). **Nav fragment (2026-08-15)**: SiteNav.vue template is now a 3-root fragment — `<header class="ws-nav">` + `.ws-nav-overlay` (z-38) + `.ws-nav-panel` (z-39) are SIBLINGS, panel/overlay outside the header so the header's stacking context can't trap them under the toggle; z-order overlay(38) < panel(39) < nav(40), toggle gets `position:relative; z-index:50` inside `.ws-nav-inner` (z-42); the panel's `.ws-lang-menu` z-60 is scoped inside the panel's own stacking context so it can never cover the nav; SiteNav's outside-click now checks `closest('.ws-nav, .ws-nav-panel')` (was `.ws-nav` only — panel siblings would have closed on inside clicks). **Section labels (2026-08-15)**: `.ws-section-label` restyled from tiny uppercase `--ink3` to `--text-2xl`/700/ink/no-uppercase (keeps `border-top` + `padding-top` + `margin: 48px 0 16px`) — Featured / Latest stories / Related stories headings now big + bold; `.ws-browse-all` (Related head) bumped to `--text-base`/600/ink with a trailing arrow SVG, `margin-bottom` dropped.
- **Pagefind + hybrid search (2026-08-17)**: site search = **Pagefind** (`pagefind` 1.5.2 devDependency; index built in buildCommand, served via `/blog/pagefind/:path*` rewrite; CSP: `script-src` self + `https://cdn.jsdelivr.net` serves NOTHING — search is a local index, no external libs; the `cdn.jsdelivr.net` img-src entry is for picsum placeholders). Index scope: `data-pagefind-body`/`data-pagefind-meta` on the article body, `data-pagefind-ignore` on `<main>` for every non-post layout — **search pages are excluded automatically** (verified: 3 posts × 3 locales = 9 indexed, 76 HTML files walked). **Hybrid flow (user decision, modal round 2026-08-17)**: the nav search **box is a button** (`SearchTrigger.vue`) that opens a **command-palette modal** (`SearchModal.vue`, ⌘K / Ctrl+K also works globally via SiteNav `onKey`; trigger shows `⌘K` on Mac/iOS, `Ctrl K` elsewhere) **and** a dedicated results page `/blog/search?q=...` (+ `/blog/{locale}/search?q=...`) — 9 `search.md` files (`layout: search` + localized title: Search/ค้นหา/검색/検索/Buscar/Cerca/Maghanap/Ara/Sök; sitemap excludes them by pattern — gen-sitemap walks the SOURCE tree, 63 URLs unchanged). **`SearchModal.vue`**: centered panel `width: min(92vw, 40rem)`, `max-height: 76vh`, `z-index: 200`, backdrop `rgba(26,26,26,0.45)`; the header IS the input (auto-focused, `--text-lg`, clear ✕ button `@mousedown.prevent` — template refs auto-unwrap so it's `inputRef?.focus()`, never `.value`); **results list is scrollable** (`overflow-y: auto` + `overscroll-behavior: contain`); hits = title (Pagefind `<mark>` via `v-html` from `highlight()`) + 2-line `-webkit-line-clamp` excerpt + category · author · date meta; 200 ms debounce, `searching` loading state, `noResults` state; ↑↓/Enter keyboard nav (`e.isComposing` guard keeps arrow/Enter from hijacking IME composition — Thai/Korean/Japanese input; `scrollIntoView({block:'nearest'})` on `ws-modal-hit-<i>`), Esc / click-outside / hit-click close; `html.ws-locked` while open + focus restore on close; footer = hints (`searchHints`) + "See all results · N" (`seeAll`) → results page; **current-language-only results** (user-delegated decision: cross-language ranking is meaningless — Thai has no spaces, CJK is character-based; all-languages can come later); on mobile the same modal opens from the full-width panel trigger (`:panel="true"` → `.ws-search-trigger-btn`), hints hidden ≤720px. `SearchBox.vue` (inline dropdown) **deleted** along with all its CSS; `.ws-search-icon` + `.ws-search-kbd` base rules kept and reused by trigger/modal. **BlogSearch.vue SSR fix (found during modal build)**: the two `watch()` calls (`route.query.q` immediate + `locale`) used to register during SSR — on the 8 locale search pages `route.value` is undefined at immediate-watch evaluation, throwing per-page `TypeError: Cannot read properties of undefined (reading 'value')` (VitePress caught it and wrote the pages anyway, but the build log was red). Fixed by wrapping both watches in `if (typeof window !== 'undefined')` — semantically neutral (server always early-returns in `run()`) and the build is now error-free. **`BlogSearch.vue`**: full list (title highlighted, 3-line excerpt with Pagefind `<mark>` via `v-html`, category link · author · date meta), client-side category filter pills (`.ws-pill` reuse, `allCategories` label), `<Pagination :total :per-page="10" :current @update>` reuse, count line (`resultsForOne`/`resultsFor` keys), empty + no-query states with `browseAll` link; SSR guard `typeof window === 'undefined'` (index absent during build) — SSR HTML = title + `searchPrompt` text, search runs on hydration. **`theme/search.js` (shared)**: `getPagefind(base)` (cached, attempt-once, **`import.meta.env.DEV` guard — dev never loads Pagefind**, `vitepress dev` SPA fallback serves `pagefind/pagefind.js` as `text/html` → module-load error; in-memory `fallbackResults` covers dev), `searchArticles(q, locale, base, limit)` → `{results, total}` (total = full pagefind count via `Number.isFinite(r.total)` fallback, limit caps rows), `postByUrl(url)` (basename-slug match against `posts.data.js` to enrich rows with title/category/author/date), `highlight(text, q)` (HTML-escaped + `<mark>`, XSS-safe) — used by both components. `theme/i18n.js` +7 keys ×9 locales: `seeAll`/`resultsFor`/`resultsForOne`/`searchPrompt`/`searchPageTitle`/`searching`/`searchHints`. No vercel.json changes needed for `/search` (existing `/blog/:path*` + `/blog/:path*/` rewrites cover it). Highlight-on-landing **deferred** (user decision, skip). Notion "Blog UI #14" Done; pagefind item #12 Done (`3bef102b-d4b1-810c-82cd-ee1caec56c3d`). Not deployed (user deploys).
- **Search box polish (2026-08-17, user test round)**: (1) **custom clear button** — native `::-webkit-search-cancel-button`/`-webkit-search-decoration` hidden (`display: none`); the native ✕ was displaced by `padding-right: 2.75rem` and renders blue in Chrome; custom `.ws-search-clear` is a wrap-internal absolute button at `right: 0.5rem` (ink3, hover ink) with an SVG ✕, shown when `query` non-empty, `@mousedown.prevent` keeps input focus, click clears + refocuses; (2) **`inputRef?.focus()` in template** — Vue template refs AUTO-UNWRAP, so `inputRef.value?.focus()` compiled to `v.value.value` (the input's *value string*) and threw "inputRef.value?.focus is not a function" (fixed; script-side `onKey`/`go()` keep `.value` — no unwrap in script); (3) **`.ws-search-field` wrapper** div (relative) inside the wrap holds input + kbd + ✕ + results dropdown, so the dropdown is anchored to the input — `left: 0; right: 0; width: auto` = EXACTLY the box width (was `clamp(20rem,34vw,26rem)` — user complained the panel was wider than the box); btn variant: field is the `flex: 1` item, go button stays a sibling, input gets `padding-right: 2.25rem`; (4) **stable input width** `clamp(14rem,24vw,19rem)`, NO focus expansion (user disliked expand-on-focus/shrink-on-blur — the results panel shrank with it); focus indicator = `border-color: var(--ink)`, width transition removed; (5) `.ws-search-icon` gets `z-index: 1` (paint-order insurance). Rebuilt + verified: click handler compiles to single `.value` (`(I=v.value)==null||I.focus()`), dist has stable width + border-color focus (the remaining `ws-search-input:focus{width:100%}` is the mobile-panel rule), icon in SSR HTML + CSS. **Superseded the same day by the modal round** (see the Pagefind bullet — `SearchBox.vue` deleted; only the `.ws-search-icon` + `.ws-search-kbd` base rules and the stable-width trigger survive in `SearchTrigger.vue`).
- **On this page TOC (2026-08-17, Blog UI #13, implemented, NOT deployed)**: `OnThisPage.vue` renders in a sticky left side rail (`.ws-side-rail`, ≥1280px) stacked above the Share rail — one column, TOC on top, "Share this article" icons below, always reachable while scrolling. On <1280px it renders as an in-flow card below the header (existing behavior). Gate: `.ws-article-body` has ≥2 h2/h3; scroll-spy `.ws-toc-on` (rAF-throttled scroll, active = last heading with top ≤ `scrollY+120`); internal scroll (`overflow-y: auto`, `max-height: calc(100vh - var(--rail-top) - 2rem)`); native `#id` jumps. **Requires `markdown.anchor` `{ permalink: false, level: [2, 3] }` in config.js** — VitePress 1.6.4 bundles markdown-it-anchor v9 whose default `level: 1` (h1-only) emits NO ids without `level` (root-caused 2026-08-17 via fixture). `scroll-margin-top: 6rem` on article h2/h3 (sticky-nav clearance). `onThisPage` label ×9 locales. SSR-safe (collects on mount). The 3 live placeholder posts have zero markdown headings so the TOC stays hidden on them; long-form articles show it.
- **SEO/a11y batch (2026-08-19, implemented, NOT deployed)**: (1) **JSON-LD + OG fix (pre-existing bug)**: VitePress `transformHtml` passes `page` as the raw markdown **file-path STRING**, not PageData — the real object is `ctx.pageData`; `page.frontmatter` was always empty, so og:title/og:description rendered empty, og:type stayed `website`, and **JSON-LD never emitted**. Fixed via `ctx.pageData` — posts now emit `BlogPosting` (headline, description, image — local `/assets/...` paths absolutized to APP_URL+BASE, datePublished/Modified, `inLanguage` per-locale, `wordCount` from markdown body via `wordCountOf()`, author/publisher Organization, mainEntityOfPage). og:image is **existsSync-guarded against `public/assets/og/`** — posts without an OG PNG fall back to `/blog/assets/blog-fallback.webp` (Group 86.png) for og:image, twitter:image AND the JSON-LD image fallback (toc-demo uses the fallback; all 66 existing og PNGs cover every other current page incl. ko post). (2) **Speculation rules**: VitePress head tuples are `[tag, attrs, innerHTML]` — a `children:` attr renders as an HTML attribute, not content; fixed to third element → `<script type="speculationrules">` prefetching `/blog/**` (moderate eagerness; CSP `'unsafe-inline'` allows it). (3) **canonical/hreflang fixes**: `coreOf(rel, locale)` strips the locale prefix + collapses the locale homepage to `''`; `pathToUrl` handles bare `index` (`/(^|\/)index$/`) → en home canonical `/blog/` (was `/blog/index/`), th home now emits x-default→en + 7 locale alternates (was 0), self-language alternate skipped, x-default = en twin when it exists. (4) **Skip link**: `.ws-skip-link` (fixed, top-left, `translateY(-300%)` → visible on `:focus`, z-60) before SiteNav → `id="main"` on `<main>`; `skipToContent` i18n ×9. (5) **Footer socials**: `SiteFooter.vue` `SOCIAL` array — X (`fa-brands fa-x-twitter` → https://x.com/wibestories) + LinkedIn (`fa-brands fa-linkedin-in` → https://www.linkedin.com/company/wibestories), `.footer-social`/`.footer-social-link` styles (32px, rgba border, hover brighten); `scripts/copy-fontawesome.mjs` now also copies `css/brands.min.css` + `webfonts/fa-brands-400.woff2` (7 files); brands.min.css added to config.js head; `followX`/`followLinkedIn` i18n ×9 (tr uses escaped `X\'te takip et`). (6) **Pagination loading**: `Pagination.vue` `loading` prop — buttons `:disabled`, nav `aria-busy`, `.ws-page-loading` pulse (`wsPagePulse` keyframes); `BlogSearch.vue` passes its real search-in-progress state. (7) **SEARCH PAGE FIX (the "nope, not working" bug)**: VitePress `useData()` does **NOT** expose `route` (verified in dist client `app/data.js`: only site/theme/page/frontmatter/params/lang/dir/localeIndex/title/description/isDark/hash) and `useRoute()`'s route is `{path, component, data}` — **no `query` ever**. BlogSearch's `route.value.query.q` threw on every load, the try/catch swallowed it, `q` stayed `''` → prompt state forever. Fixed: watch `() => new URLSearchParams(location.search).get('q') || ''`. (8) **Fallback image TEST**: `assets/images/BLOG/Group 86.png` converted to `blog-src/.vitepress/public/assets/blog-fallback.webp` (1920×1308, yuv420p, libwebp q80, 265KB) — wired ONLY on `posts/toc-demo.md` (`image: /assets/blog-fallback.webp`); **all four image renderers** (BlogArticle + PostCard + FeaturedPost + BlogSearch) now use the shared `imgSrc()` helper (`theme/utils.js` — absolute URLs pass through, else `withBase`; frontmatter `image:` is NOT base-processed by VitePress, raw `/assets/...` 404'd). Same webp doubles as the og:image/twitter:image/JSON-LD-image fallback for posts lacking an OG PNG (existsSync guard in config.js). **brands.min.css + fa-brands-400.woff2 also copied into `public/assets/fontawesome/`** (2026-08-19) so a bare `vitepress build` is self-sufficient — copy-fontawesome.mjs stays for the other 5 files (idempotent, same destination). Auto-fallback for image-less posts in the card components still waits user's visual approval of toc-demo. **All CDP-verified** (Edge headless + native WebSocket, no playwright): search `?q=voice` → hero + 2 hits; article → fallback img 1920×1308 via `/blog/` base, rail/TOC/share/skip/socials(2 FA icons)/spec rules/JSON-LD all present; th post → JSON-LD `th-TH`; homepage + search pages → zero 4xx (note: `vitepress preview` indexes dist at startup — restart it after any rebuild, new hashed assets 404 until then; same quirk as `vercel dev`).
- **Intercom alignment (2026-08-05, user-specified, keep)**: mirrors the Intercom blog layout (`intercom-blog-frontend.css` at repo root is the reference). Spacing tokens in `:root` (`--space-2…--space-160` = `.125rem`…`10rem`); `.ws-container` 880→**1050px**, sides 12px mobile → 24px ≥768px. Page paddings on `<main>` via Layout.vue classes: `.ws-page-home` 80px/12px → ≥768px 120px/24px/96px → ≥1280px 160px top/96px bottom; `.ws-page-category` 80px/12px/48px → ≥768px 160px/24px/96px; `.ws-page-post` **40px/12px/40px → ≥768px 64px/24px/96px** (tightened 2026-08-09 from 80px/160px top — article top gap is now 40px mobile / 64px desktop; masthead/category-head/article-top own top paddings zeroed so page padding owns the gap); containers on those pages get `padding-left/right: 0` (page padding covers the sides). Category pills (`.ws-pills`) and month strip (`.ws-months`) are full-width outside the container with side padding matching the page (12px mobile → 24px ≥768px). Post pages split: `.ws-article-main` narrow 45rem (720px) + wide `.ws-container` foot for RelatedPosts/CTA (Intercom `__footer` pattern). Footer `margin-top: 64px` → 0.
- **Cards (Intercom list style)**: `PostCard.vue` is now `<article class="ws-card">` — whole-card anchor removed (nested-anchor defect: a clickable `.ws-cat` `<a>` inside a card-`<a>` is invalid HTML); separate links: `.ws-card-title`, `.ws-card-img` (`330px`, `aspect-ratio: 5/3`, `object-fit: cover`), `.ws-cat` (locale-aware category href, same pattern as CategoryPills), `.ws-read-more`. **No author row in list cards** (Intercom has none). `.ws-list` gap 0; `.ws-list .ws-card` `padding: 24px 0` + `border-bottom` + `:first-child` `border-top`; hover `background: var(--cream2)`. Meta rows: `.ws-cat` (animated underline via `::after` `scaleX(0→1)`, 2px) + "Published on {date}" (`publishedOn`), then clock SVG + `readMin` (computed in `posts.data.js` from `bodyText`, words/200) + "Read more" (`readMore` i18n key added to all 9 locales). Mobile (≤768px) stacks column, **image stays visible** (user decision). **Related variant (2026-08-15)**: `variant="related"` renders image-first (`.ws-card-img` full-width, `aspect-ratio: 5/3`, `order: -1` — base rule swapped from the old 7–9rem clamped strip), then "Published {date}", 2xl bold title, then `.ws-card-byline` = author (`.ws-card-author` avatar + name) · clock read-time on ONE line; `.ws-related .ws-card-body` padding `16px 0 0`. Used by `RelatedPosts.vue` (`variant="related"`, dropped `show-author`).
- **Featured card** = Intercom `.c-posts-card-latest`: `<article class="ws-featured-card">`, column, **left-aligned everywhere** (2026-08-15: `text-align: left` + `.ws-featured-author` `flex-start` at base, mobile no longer centers) → ≥1024px `row-reverse`, image 65% / body 35%, gap 32px, `margin-bottom` 96px; **author row = 28px initials circle `.ws-avatar` (`border-radius: 50%`, `background: var(--ink)`, letter initial) · name · clock `readMin` read-time on ONE line** (2026-08-15 merged the old separate `.ws-featured-meta` row away), "Read more" is a standalone link after the author row; title/excerpt; `.ws-featured-nothumb` when no image. `.ws-avatar` also used in the article header meta row.
- **Pagination**: `.ws-pagination` `margin-top` 48px → 96px ≥768px; buttons `min-width/height: 40px`, hover `background: var(--cream2)`.
- **Language select**: `.ws-lang-select` in `LanguagePills.vue` binds `:value` to the current locale's **href** (`/blog/`, `/blog/th/`, …) so the select displays the current language name instead of blank — do NOT bind to the locale code while option values are hrefs. Select is flat (transparent, 1px border, 4px radius), not pills.
- **Month strip (verified 2026-08-15)**: `MonthStrip.vue` renders a **`.ws-filter-label` ("Filter by" — `filterBy` i18n key added to all 9 locales)** above two control sets — `.ws-year-row` year buttons (`.ws-year-on` when selected) + `.ws-month-row` month buttons (`.ws-month-on` for the All button and the selected month), hidden ≤768px, and `.ws-select-row` with **two custom `role=listbox` dropdowns (`.ws-mfilter-btn` + `.ws-mfilter-menu`, chevron rotates when `[aria-expanded=true]`, options disabled when that month has no posts; replaced the old native `<select>`s)** on mobile; dropdowns close on outside click and `Esc` (same pattern as LanguagePills). Selection flows through the `selected` prop (`"YYYY"` or `"YYYY-MM"`; `BlogHome` computes the default = current month if it has posts, else the latest month with posts) and `update` emits. Month labels via `Intl.DateTimeFormat` with a locale map (th-TH/ko-KR/ja-JP/es-ES/it-IT/fil-PH/tr-TR/sv-SE). There is NO `auto` prop and no `?m=` query param support — category pages do not use MonthStrip at all. Dates arrive from the data loader as ISO `YYYY-MM-DD` strings, not Date objects — `new Date(x)` works on them. **Homepage position (2026-08-15)**: the strip + label moved INSIDE the container ABOVE the Featured card (was below it — filters were invisible under the fold); `CategoryPills` + `CategoryDropdown` removed from `BlogHome.vue` entirely (CategoryDropdown still used by `BlogCategory.vue`; CategoryPills.vue now unused but kept).
- **Article footer stack (verified 2026-08-09)**: `BlogArticle.vue` footer above the AI footer CTA: `.ws-share-row` ("Share" label + single `.ws-share-btn` opening the `ShareMenu` dropdown — see Share button bullet), then `.ws-disclosure` (translated string), `.ws-improve` link to the Tally form `https://tally.so/r/WO6B8Q?article=<title>` (title from frontmatter; `improve: false` in frontmatter switches the label to `improveFinal`, else `improveOpen`) — this Tally form is the Way B intake path. Conditional `.ws-contributors` row (from frontmatter `contributors`, initials circles) below the improve link. **The `.ws-signature` is no longer in the footer stack** (2026-08-09) — it now renders at the END of the article body (just above the `cta-section`), a plain centered translated string. All footer-stack i18n keys (`disclosure`/`shareLine`/`shareButton`/`improveOpen`/`improveFinal`/`copied`) exist in all 9 locales.
- **Share button (2026-08-09, replaced Web Share dialog)**: `ShareMenu.vue` component — `.ws-share-menu` dropdown (4px radius flat panel, `--ink` bg / cream text, 10px padding, `z-index: 200`) opened by `.ws-share-btn` (footer share row) and `.ws-share-top` (header icon). Targets: Copy link (clipboard, 2s "Copied" flip), LinkedIn, X, Facebook, Bluesky — **plain native share URLs** (`https://www.linkedin.com/sharing/share-offsite/?url=…`, `https://twitter.com/intent/tweet?text=…&url=…`, `https://www.facebook.com/sharer/sharer.php?u=…`, `https://bsky.app/intent/compose?text=…`), **no third-party JS/scripts** (CSP clean). `shareOpen`/`shareClose`/`shareCopy`/`shareMenuCopy`/`shareMenuLinkedIn`/`shareMenuX`/`shareMenuFacebook`/`shareMenuBluesky` i18n keys in all 9 locales. Share URL built from the page's own `page.relativePath` + locale prefix; dropdown closes on outside click and `Esc`. No `navigator.share` call anymore — the menu is the only share path.
- **Article share bars (2026-08-15)**: `ShareBar.vue` (variants `rail`/`top`/`foot`) is **icon-only in every variant** — all visible labels (`ws-sharebar-label`, `ws-sharebar-btn-label`) removed (dead CSS deleted); every button carries an `aria-label` (`shareX`/`shareLinkedIn`/`shareFacebook`; copy flips between `shareCopy`/`copied`). The desktop rail (≥1280px, `.ws-sharebar-rail`) is a fixed left rail whose **top is JS-driven in `BlogArticle.vue` `updateRail()`**: `rail.style.top = max(96px, article column top)` (rAF-throttled, on scroll/resize/mount) so it hugs the article body, and it fades (`.ws-rail-hidden`) once the signature passes it. Top/foot bars: `.ws-sharebar-top`/`.ws-sharebar-foot` (flex rows <1280px, `padding-right: 8px`).
- **Categories (overhauled 2026-08-09)**: the `CATEGORIES` dict in `theme/i18n.js` has 6 keys ×9 locales — `wibes-news` ("Wibe's News"), `voice-dictation` ("Voice Dictation"), `tech-behind` ("Tech Behind"), `confluence` ("Confluence"), `user-stories` ("User Stories"), `cultural-mosaic` ("Cultural Mosaic") — array order IS the display order (Wibe's News first). Display names are IDENTICAL in every language — the readable slug words (e.g. "Confluence" is "Confluence" in th/ko/ja too, never translated; user rule: slug and display name are the same). Category names are NEVER translated - even when a good translation exists (names appear in many places: homepage category row, article metadata, category page title; user rule: English always). Only the page descriptions are localized per locale - each of the 8 locales (th/ko/ja/es/it/tl/tr/sv) has its own translation, and a translation must back-translate to the same meaning and intention as the English one-liner (words may change, meaning must not). Category pages now DISPLAY their frontmatter `description` under the title (`.ws-cat-desc`, added 2026-08-09; old pages hid it). The old slugs (`voice-recording-tips`/`stories-traditions`/`tech-ai`/`occasions-celebrations`/`product-news`/`languages-culture`, and later `seasonal-moments`/`words-language` superseded by `confluence`/`cultural-mosaic` in the finalized 2026-08-09 table) were deleted with NO redirects — article frontmatter and URLs use the new slugs only, do not re-add the old ones. Category page descriptions ARE the user's final one-liners (canonical copy in `content-guide/CATEGORIES.md`; translated per locale in the 54 category files).
- **Phase 1 pipeline (verified 2026-08-09)**: `scripts/fetch-content.mjs` is a **no-op** (exit 0) unless `CONTENT_TOKEN` + `CONTENT_REPO` env vars are set (Vercel) — then it git-clones the private content repo and copies `posts/` + the 8 locale `*/posts/` dirs into `blog-src/`. It does NOT read `content-guide/` and never regenerates categories. `scripts/gen-sitemap.mjs` runs AFTER `npx vitepress build blog-src` and writes `blog-src/.vitepress/dist/sitemap.xml` from the built HTML (excludes 404 + `*-en` companion pages; priorities home 1.0 / locale homes 0.9 / categories 0.7 / articles 0.6) — the repo-root `sitemap.xml` (27 URLs) is a separate hand-maintained file. **`INCLUDE_POSTS = false` in gen-sitemap.mjs (deliberate)**: article pages are excluded from the blog sitemap until real articles ship — flip to `true` then. **Content on disk (2026-08-24, BLANK STATE)**: all placeholder posts deleted (`posts/welcome-to-wibe-and-wonder.md`, `th/posts/mother-tongue-thai-placeholder.md`, `ko/posts/clearer-voice-korean-placeholder.md`, `posts/toc-demo.md`) and the git-ignored `_backup-blog-articles/` folder (27 AI drafts, 9 locale dirs) **permanently deleted** — blog-src now has ZERO posts; homes + 54 category pages render empty by design. Real articles come exclusively from the private content repo (`CONTENT_REPO` = `Yellow-Green-Labs/wibe-blog-content` slug format + `CONTENT_TOKEN`, both set in Vercel env 2026-08-24). `blog-src/content-guide/` holds the pipeline docs (TEMPLATE, CATEGORIES, LANGUAGES + 5 topic briefs). **TEMPLATE.md `ps:` field (2026-08-24, REQUIRED)**: every article carries a short P.S. in its own language; `BlogArticle.vue` renders it as `.ws-ps` (label + text) after the body, before the article footer; empty/missing ps renders nothing. **Way B robot (phase 2)**: planned autonomous agent that publishes content-repo articles and processes emails/updates via the Tally `?article=` prefill. Before deploying run the exact Vercel buildCommand chain locally (fetch-content no-ops, but the build + sitemap + docs must pass).
- **Key files**: `blog-src/.vitepress/config.js` (base `/blog/`, cleanUrls), `theme/custom.css` (all design rules + `--cream: #fffff6`), `theme/i18n.js` (LOCALES/CATEGORIES/UI dictionaries in 9 locales, `useI18n`/`useLocale`/`fmtDate`, `mission` dict), `theme/data/posts.data.js` (article data), `theme/search.js` (Pagefind + fallback search, shared), `theme/scroll-lock.js` (lockScroll/unlockScroll — shared body-lock with `--sbw` scrollbar-width compensation, used by SearchModal + SiteNav mobile panel), `theme/components/` (BlogHome, BlogArticle, BlogCategory, BlogSearch, PostCard, FeaturedPost, CategoryPills, LanguagePills, MonthStrip, Pagination, RelatedPosts, OnThisPage, SearchModal, SearchTrigger, BackToTop, ShareMenu, SiteFooter, SiteNav, Layout), `index.md` + per-locale `index.md`, `search.md` + per-locale `search.md` (9 search pages, `layout: search`), `posts/*.md` (0 posts — blank state 2026-08-24), `categories/*.md`, `content-guide/` (pipeline docs). Blog is **untracked in git** as of 2026-08-05 (`git status` shows `?? blog-src/` and ` M vercel.json`). `.gitignore` covers `blog-src/.vitepress/.temp/`, `cache/`, `dist/` (same pattern as docs-src). **`_backup-blog-articles/` DELETED 2026-08-24** (was git-ignored phase-1 drafts; user-approved permanent removal). Blog public assets (2026-08-09): `blog-src/.vitepress/public/assets/cta-bg.webp` (subtle card-grid background image for the article CTA section, About-page style) + `YGL-L-B.png` (footer logo) + `BLOG-LABEL.png` (masthead logo, served via the vercel.json `/blog/BLOG-LABEL.png` rewrite added 2026-08-24 — dist-root files need explicit rewrites because the `/blog/:path*` catch-all appends `.html`).
- **Blog link in every site menu (2026-08-24)**: "Read Articles" in the app hamburger (`wisprstories.html`) + footer menu (`global/footer-menu.js`) now points to `/blog/` (was `https://medium.com/`, icon `fa-brands fa-medium` → `fa-solid fa-newspaper`, `target="_blank"` dropped — internal link now). A static `Blog` link (`href="/blog/"`, after About) was added to the landing `lp-nav` and to the desktop `nav-links` + hamburger of `pricing.html`, `about.html`, `features.html`, `language-stats.html` (all static English, matching those pages' hardcoded nav pattern — no i18n keys). 404.html has no menu (skip); the blog's own SiteNav is the blog (skip).
- CSP (vercel.json): `img-src` allows **`https://fastly.picsum.photos`** (the picsum CDN — picsum.photos itself 302s there; article images are picsum placeholders; the plain `picsum.photos` origin alone produced CSP violations, fixed 2026-08-09) and `https://cdn.jsdelivr.net` (search uses a local index — no external libs). **Blog rewrite order (2026-08-09)**: explicit single-file rules for `/blog/*.png|jpg|jpeg|webp|gif|ico|css|xml|js` + `/blog/assets/*` + `/blog/vp-icons.css` come BEFORE the `/blog/:path*` rewrite — Vercel rewrites are ordered, and the catch-all previously swallowed these files (404s in the browser with correct page HTML). The `/blog` (no trailing slash) → `/blog/` redirect stays. **Trailing-slash deep URLs (2026-08-09, v0.11.30.x)**: `:path*` captures a trailing `/`, so `/blog/categories/wibes-news/` hit `categories/wibes-news/.html` → 404 (VitePress cleanUrls links are slash-less, so clicks worked; only typed/copied slash URLs broke). Fixed with explicit `/blog/:path*/` and `/docs/:path*/` → `.html` rewrites placed before the slash-less catch-alls (docs had the same latent bug).



<!-- agsync: nc: last-run 2026-08-24; Post-session note (2026-08-24, BLOG BLANK STATE + BLOG NAV EVERYWHERE + PS FIELD, implemented, NOT deployed): user added CONTENT_REPO (Yellow-Green-Labs/wibe-blog-content) + CONTENT_TOKEN to Vercel env. (1) Deletions (user-approved, no assets touched): _backup-blog-articles/ (27 AI drafts, git-ignored, permanent), 4 placeholder posts (toc-demo untracked; welcome/mother-tongue/clearer-voice git rm staged, NO commit per standing rule), stray blog/404.html duplicate folder. .gitignore entry removed. Blog-src = ZERO posts (blank state); homes + 54 category pages render empty; build green. (2) vercel.json: /blog/BLOG-LABEL.png rewrite added (masthead logo 404 fix — catch-all appends .html, dist-root files need explicit rewrites). (3) Blog link everywhere: Read Articles medium.com -> /blog/ in wisprstories.html hamburger + footer-menu.js (fa-brands fa-medium -> fa-solid fa-newspaper, target=_blank dropped); static Blog link after About in lp-nav + nav-links/hamburgers of pricing/about/features/language-stats.html (no i18n keys — those navs are static English). (4) PS field: TEMPLATE.md ps: REQUIRED field + mandatory rule; BlogArticle.vue renders .ws-ps (P.S. label + text) after body/signature, before article footer; custom.css styles (top hairline, ink2); empty ps renders nothing (v-if). Temp-post verified in dist then removed. (5) INCLUDE_POSTS=false in gen-sitemap.mjs is deliberate — flip true when real articles ship (flagged to user). Sitemap 63 = 9 homes + 54 categories. (6) Verified: /blog/ 200 blank masthead, BLOG-LABEL.png 200, category pages 200 empty, zero medium.com refs, /blog/ counts 2/1/2/2/2/2 across the 6 files. Pending user: add test post to content repo (posts/ or <lang>/posts/, full frontmatter incl. ps) -> vercel --prod -> verify live. Committing still deferred (user commits later). Follow-up same session (TEMPLATE REWRITE): content-guide/TEMPLATE.md rewritten from scratch — removed gist pipeline mechanics (approval gate, gist footer, GIST_APPROVE_BASE/REJECT_BASE endpoints, Strategist/Quill personas, gist description prefix), replaced with a clean fill-in skeleton (YAML front matter with inline comments + body structure with placeholders + rules section). File destinations (posts/<slug>.md etc.) kept as a brief header note. Gist/Strategist/Quill count = 0 in template. Post-session note (2026-08-19, PRICING MODAL RECONSTRUCTION, implemented, NOT deployed): accidental comment-removal edit corrupted global/pricing-modal.js (467→323 lines, ~144 lines deleted: handleKey, handleEmail, _disposableDomains, key panel, key/email listeners+whitelists, hidden Plans tab). Reconstructed from HEAD verbatim while preserving gift-code work (handleGiftCode, gift tab/panel, instanceof guards, slideTo fix). main.css overlays import bumped to ?v=20260818-v0.11.38.0. CDP fix probe passes: 0px drift, crash guard no-ops, wave animation restored, zero window errors. Key files updated: global/pricing-modal.js, global/styles/main.css. AGENTS.md pricing-modal bullet updated with reconstruction note.
Post-session note (2026-08-19, BLOG SEO/A11Y BATCH + SEARCH PAGE FIX, implemented, NOT deployed): all five approved improvements done + CDP-verified (Edge headless + native WebSocket, no playwright; scripts in AppData\Local\Temp\opencode). (1) JSON-LD root cause: VitePress transformHtml passes `page` = the raw md file-path STRING (ctx has `pageData` separately) -> page.frontmatter always empty -> og:title/og:description empty, og:type=website, JSON-LD NEVER rendered (pre-existing bug; user never saw it). Fixed via ctx.pageData: posts emit BlogPosting with headline/description/image (local /assets absolutized via APP_URL+BASE)/datePublished/dateModified/inLanguage (per-locale, en-US + th-TH verified)/wordCount (wordCountOf reads markdown body)/author+publisher Organization/mainEntityOfPage. (2) Speculation rules: VitePress head tuple is [tag, attrs, innerHTML] - a children: attr renders as an HTML attribute (verified in renderHead source); fixed to third element; dist shows `<script type="speculationrules">{"prefetch":[{"source":"document","where":{"and":[{"href_matches":"/blog/**"}]},"eagerness":"moderate"}]}</script>` (CSP unsafe-inline allows). (3) canonical/hreflang: coreOf strips locale prefix + collapses 'index'->''; pathToUrl handles bare index (/(^|\/)index$/) -> en home canonical now /blog/ (was /blog/index/), th home gets x-default->en + 7 locales (was 0), self-language alternate skipped. (4) Skip link (.ws-skip-link fixed, focus-revealed, z-60) -> #main; skipToContent x9. (5) Footer socials: X + LinkedIn FA-brand icons (x-twitter/linkedin-in), SOCIAL array in SiteFooter, footer-social styles; copy-fontawesome.mjs now 7 files (brands.min.css + fa-brands-400.woff2), brands.min.css in config head; followX/followLinkedIn x9 (tr escaped X\'te takip et). (6) Pagination loading prop: disabled buttons + aria-busy + wsPagePulse; BlogSearch passes real search state. (7) SEARCH PAGE FIX (the "nope not working"): useData() has NO route key (dist client app/data.js: site/theme/page/frontmatter/params/lang/dir/localeIndex/title/description/isDark/hash) and useRoute() route = {path, component, data} - no query EVER; BlogSearch's route.value.query.q threw, try/catch swallowed it, q always '' -> prompt forever. Fixed: watch new URLSearchParams(location.search).get('q'); CDP: /blog/search?q=voice -> hero + 2 hits. (8) Fallback image TEST: Group 86.png -> blog-fallback.webp (1920x1308, 265KB) on toc-demo.md only; BlogArticle imgSrc() withBase fix (raw /assets 404'd without base - frontmatter image is NOT base-processed by VitePress); rollout to PostCard/FeaturedPost/BlogSearch waits user visual approval of toc-demo. Full chain green (fetch-content no-op, blog build 2-3s, pagefind 1.5-1.7s 3 pages, gen-sitemap 63, docs build 2-4s). Dev server restarted on 5174 for user review. Changelog: blog rounds are not versioned in docs-src/updates/changelog.md (app changelog only) - AGENTS.md bullet is the record. Not deployed (user deploys). Follow-up same session (BLOG 404 WIRING): root 404.html (untracked, user-made, blog variant via its JS branch for /blog/ paths) was already served by Vercel as the custom error page for ALL unmatched routes, so missing /blog/* URLs (catch-all rewrite misses) already rendered the blog variant; the only gap was the explicit /blog/404 rewrite pointing at blog dist's VitePress-DEFAULT 404.html (no custom design, no variant) - vercel.json /blog/404 destination changed to /404.html (one line, no rebuild needed; blog dist/404.html stays as dead generated output). Docs (/docs/404) deliberately left untouched - the custom page has no /docs/ branch. Post-review fix round (same session, critical self-review findings): (1) og:image was hardcoded with NO existence check - only toc-demo lacked an OG PNG (ko post's og/ko/posts png exists; my review claim was half-wrong, verified via full og tree listing) - config.js now existsSync-guards public/assets/og/<rel>.png and falls back to /blog/assets/blog-fallback.webp (Group 86.png, the user's designated fallback) for og:image + twitter:image + JSON-LD image fallback; dist-verified: toc-demo -> fallback, ko -> real PNG. (2) brands.min.css + fa-brands-400.woff2 copied into blog-src/.vitepress/public/assets/fontawesome/ (bare `vitepress build` now self-sufficient; copy-fontawesome.mjs kept for the other 5, idempotent same-destination); bare-build proven by deleting dist copies + rebuilding. Known accepted: /blog/404 rewrite serves 200 (Vercel rewrites can't set status; corner URL, noindex); root 404.html + blog-src untracked in git (deploy via local vercel --prod only; committing deferred by user). Post-deploy checklist: /blog/404 (200 + custom page), /blog/definitely-not-a-page (404 + blog variant), toc-demo og:image 200, toc-demo presence in prod (fetch-content overwrite behavior).
Post-session note (2026-08-18, PRICING MODAL FIX BATCH + NOTION CLEANUP, implemented, NOT deployed): user approved the 3 latent pricing-modal fixes (don't break anything). global/pricing-modal.js: overlay mouseenter/mouseleave CTA-wave handlers guard `e.target instanceof Element ? e.target.closest(".pricing-cta") : null` (target can be `document` when the pointer leaves the window -> `.closest` TypeError); slideTo() re-anchors the toggle slider instantly after the 120 ms cost-text swap (transition:none + reflow, mirrors pricing-page.js). overlays.css: `.pricing-pro-cost` now `width: 120px` (keeps flex-shrink:0; min-width:90px) so the Best Value detail can never expand the cost block and shift the track. main.css overlays import bumped to ?v=20260818-v0.11.38.0 (cache-bust). CDP-verified: 0 px drift on pricing page + modal, crash guard no-ops, wave enter/leave + backdrop + Escape all work, zero window errors. Note: modal Plans panel + Plans tab are display:none (show() always opens the Gift tab) -> drift/crash fixes are latent-safety today; cost-width CSS is live wherever the picker renders. Notion Project Tasks cleanup: Key Status Modal -> Done, Gift card amber banner -> Done, "Cleanup - remove pricing-modal.js/key-status-modal.js/resend-key.js" -> Discarded (premise false: all three live; global/resend-key.js doesn't exist, api/resend-key.js is the recovery endpoint the modal calls). Changelog v0.11.38.0 Fixed section updated. Deploy still deferred (user deploys).
Post-session note (2026-08-17, BLOG SEARCH ROUND - PAGEFIND + HYBRID SEARCH, implemented, NOT deployed): user approved hybrid flow (dropdown preview + dedicated results page) + bigger box with kbd hint; highlight-on-landing deferred (user decision, skip). (1) pagefind 1.5.2 devDependency; vercel.json buildCommand now fetch-content -> vitepress build blog-src -> pagefind --site dist -> copy-fontawesome -> gen-sitemap -> docs build; /blog/pagefind/:path* rewrite added. (2) Index scope: data-pagefind-body/meta on article body, data-pagefind-ignore on <main> for every non-post layout - search pages excluded automatically (verified: 76 files walked, 3 pages indexed, 3 languages). (3) Dev-mode fix: vitepress dev SPA fallback serves pagefind/pagefind.js as text/html -> import.meta.env.DEV guard in theme/search.js getPagefind (dev never loads Pagefind; in-memory fallbackResults covers dev); prod bundle re-verified. (4) theme/search.js (shared): getPagefind (cached, attempt-once), searchArticles(q, locale, base, limit) -> {results, total} (Number.isFinite(r.total) fallback), postByUrl (basename-slug match vs posts.data.js), highlight() (escaped + <mark>, XSS-safe). (5) SearchBox.vue rewritten: PREVIEW_LIMIT=6, seeAll()/resultsHref() via withBase, Enter-without-active -> results page, mobile go-button navigates too, kbd hint badge (.ws-search-kbd; 'Ctrl K' SSR default, CmdK on Mac via onMounted; hidden while typing/focused/panel). (6) BlogSearch.vue (new): results page, SSR guard (typeof window === 'undefined'), title + 3-line excerpt (pagefind <mark> v-html) + category link + author + date meta, client-side category filter pills (.ws-pill reuse, allCategories), Pagination reuse (10/page), count line (resultsForOne/resultsFor), empty + no-q states (browseAll). (7) 9 search.md files (layout: search; localized titles Search/\u30e1\u30c3\u30c8/\u30e1\u30c3\u30c8/Buscar/Cerca/Maghanap/Ara/Sök); sitemap excludes them by pattern (63 URLs unchanged); no vercel.json chang...
-->

## Notion MCP (global OpenCode config)

Notion MCP is configured at the global level (`C:\Users\srini\.config\opencode\opencode.json`) so it's available in **any** project directory (Wispr Stories, content-orchestra, etc.).

- **Config**: Remote MCP server at `https://mcp.notion.com/mcp`
- **Auth**: OAuth (completed once, tokens stored at `~/.local/share/opencode/mcp-auth.json`)
- **Capabilities**: Create, read, update, search Notion pages and databases conversationally
- **Limitations**: Requires user OAuth (not headless). Autonomous agents running via `orchestrator.js` (OpenRouter API) do NOT have MCP access — they need the API-based `notion.js` path.
- **`opencode-ai` CLI**: Installed globally (`npm install -g opencode-ai`) for `opencode mcp auth` commands
- **Desktop app**: Shares global config; restart required after config changes

## OpenSEO MCP (project-scoped)

- `opencode.json` at the repo root registers the **OpenSEO** remote MCP (`https://app.openseo.so/mcp`, `type: "remote"`) for **this project only** — project config deep-merges with the global config, so Notion stays available everywhere and OpenSEO appears only when opencode runs inside this repo. No API key in config; first connection triggers OpenSEO browser OAuth login, tokens stored at `~/.local/share/opencode/mcp-auth.json` (same flow as Notion). Added 2026-08-10.

## Loops — RETIRED (2026-08-17)

Loops was **dropped entirely on 2026-08-17** — all 12 email types (transactional + marketing) run on **Resend only** (decision 12, `frontlogs/EMAILS.md`). Decisive rationale: Loops accepts **LMX/MJML only, no raw HTML** — the arched-card occasion design needs raw HTML (background-image / overlap / VML) and could never move there; plus confusing UI and no advantage over Resend's open/click tracking + free tier. The record below is kept for historical context only.

- **There is NO usable Loops MCP.** The entry added 2026-08-15 (`https://mcp.loops.so`, `type: "remote"`) was **removed 2026-08-16** after investigation: (1) unauthenticated it exposes only Loops' **Mintlify docs-search MCP** — read-only (`search_loops`, `query_docs_filesystem_loops`, `submit_feedback`); (2) its Bearer auth is RFC 9729 protected-resource metadata (`authorization_servers: ["https://app.loops.so"]`) that opencode's OAuth auto-detection cannot complete (log: `"server unavailable" status=failed`), and the Loops API key is **rejected** as the bearer (verified live 401). The **full-featured** Loops MCP (contacts/campaigns/transactional) exists but is documented for **Claude Code / Claude Desktop** browser OAuth only — opencode cannot use it.
- **Loops REST API** (`https://app.loops.so/api/v1`, `LOOPS_API_KEY`): components/workflows/campaign-groups/contacts/events worked pre-domain (verified 2026-08-16); campaigns + transactional-emails + the email-message content layer were gated 400 until a sending domain. **No raw HTML support** — the dealbreaker.
- **Cleanup (user)**: close the Loops account — asset IDs in `frontlogs/emails/loops-built.json`; probe leftovers `__probe_ws_workflow`, `__probe_ws_cg`, `ws_probe` pattern, `ws-probe-milestone@example.com` contact; remove `LOOPS_API_KEY` from Vercel env. `scripts/build-loops-emails.mjs` was deleted 2026-08-17.

## Email program — Resend-only (since 2026-08-17)

- **All 12 email types on Resend** (transactional + marketing). Classification rule from the split era still documented in `frontlogs/EMAILS.md`: *"Would it be wrong if an unsubscribed user never saw it?"* — transactional (Pro Welcome, Key Resend, Vault T+30/T+60/T+90, occasion reminders) must reach unsubscribed users; marketing (Milestone, Founder Letter, Expiry T-7/T-1/T+7, Feature Announcement) carries the one-click unsubscribe footer.
- **Addresses (user's choice, locked)**: `hello@mail.wibestories.app` (transactional) + `stories@mail.wibestories.app` (marketing), ONE shared subdomain `mail.wibestories.app` (combined SPF, separate DKIM selectors). Domain purchase + DNS = user task (Phase B). Current senders = `onboarding@resend.dev` placeholder.
- **Marketing program built (2026-08-17, NOT deployed, all dormant)**: shared shell `lib/marketing-email.js` (cream `#fff8eb`, amber CTA, Contact us / Help us improve footer links, unsubscribe footer, 8s timeout) + templates `lib/milestone-email.js` / `lib/founder-letter-email.js` (approved copy, no em dashes) / `lib/expiry-email.js`; crons `api/cron/milestones.js` (rewritten, 05:00 UTC) + `api/cron/expiry-emails.js` (new, 06:00 UTC); manual trigger `api/send-founder-letter.js` (monthly dedupe `wispr:founder:YYYY-MM`); tracking webhook `api/webhooks/resend.js` → Neon `email_events` (Svix-signed; answers the "email open tracking" question). Gates: `MARKETING_ENABLED=1` (marketing crons + founder letter), `VAULT_CHAIN_ENABLED=1` (vault chain).
- **Dedupe keys**: `wispr:milestone:last:<key>` (existing, 90d cooldown), `wispr:expiry:{t-7|t-1|t+7}:<email>` (10y), `wispr:founder:YYYY-MM` (10y, set only on zero-error runs).
- **Phase B (user, domain-gated)**: buy `wibestories.app` + DNS (SPF/DKIM/MX), set real senders, sender logo (BIMI), create the Resend webhook with `RESEND_WEBHOOK_SECRET`, test-sends, passinbox tests, then arm `MARKETING_ENABLED`. `RESEND_API_KEY` not in `.env`.
## Project Tasks database (Notion)

A Notion database called **"Project Tasks"** was created on 2026-07-29 as the single source of truth for all remaining work. It lives under the "WIBE_STORIES frontlogs" page.

- **Database URL**: `collection://6c9a1552-956a-4cc4-a965-1616d039403c`
- **Items**: 110 total (109 Pending, 1 Discarded) across 15 categories
- **Views**: 📋 PENDING (filtered, grouped by Category), 📅 TIMELINE (by Target Date), 📊 ALL ITEMS (full list)
- **Workflow**: All new items go into the database. The old PENDING toggle list on the page is legacy/superseded.
- **Session start**: Fetch the Notion page to check the database for new/updated items. Also check the **C_WS-updates** brain dump section for any new unchecked items.
- **Updates**: Update the database row when completing/pending items. Do NOT update the page's static sections.
- **Schema**: Item (title), Category (15 options), Status (Pending/In Progress/Done/Discarded), Priority (Critical/High/Medium/Low), Effort (Small/Medium/Large/XLarge), Source (PENDING/DECISIONS/GAP/ROADMAP/EMAILS/SERVICES/C_WS-updates), Start Date, Target Date, Notes

## Frontlogs (Notion page)

Frontlogs are maintained on the Notion page "WIBE_STORIES frontlogs" (not in local files).

- **URL**: https://app.notion.com/p/3abf102bd4b1810d8da8c481d4a58fdb
- **Source of truth**: Notion page (local copies deleted). The **Project Tasks** database (on the same page) is the source of truth for work tracking. The static PENDING/DECISIONS/USEFUL_SERVICES sections are preserved for reference but superseded.
- **Session start**: At the start of every session, fetch this Notion page and check the Project Tasks database for new/updated items. Also check the **C_WS-updates brain dump section** — the AI MUST read this section every session even if not explicitly told to. Check for unchecked items and add them to the database.
- **Updates**: When completing a pending item, update its Status in the Project Tasks database to "Done" and add a note about completion. Also check the C_WS-updates brain dump section for any new unchecked items the user may have added.
- **User can update**: Anytime from anywhere — the AI picks up changes at session start
- **Migrated files**: PENDING.md (cleaned, only pending items), DECISIONS.md (grouped by domain), USEFUL_SERVICES.md (same structure, trimmed), C_WS-updates.md (verbatim)

<!-- agsync: nc: last-run 2026-08-25; Post-session note (2026-08-25, CLERK PROXY TOP-LEVEL FUNCTION, committed + deployed): the subdirectory catch-all api/clerk-proxy/[...path].js only matched single-segment paths on Vercel (multi-segment /npm/@clerk/... 404'd). Replaced with a top-level edge function api/clerk-proxy.js that takes the path via ?clerkPath= query param; vercel.json rewrite updated to /api/clerk-proxy?clerkPath=:path*. Proxy forwards to frontend-api.clerk.dev with Clerk-Proxy-Url + Clerk-Secret-Key headers, redirect:'manual' passes Clerk's 307 version-redirects back so the browser re-enters the proxy at the exact-version path. Live-verified: /__clerk/npm/@clerk/ui@1/dist/ui.browser.js -> 307 -> version-resolved -> 200 application/javascript 140KB; sign-in click renders Clerk modal (clerkClassCount 56). E2E modal check: email input present, phone absent, but Google + Microsoft social buttons still showing (user must disable in Clerk Dashboard for email-only). Key files updated: api/clerk-proxy.js (NEW), api/clerk-proxy/[...path].js (DELETED), vercel.json. AGENTS.md: Clerk auth wiring bullet updated to proxy-mode architecture; api/clerk-proxy.js added to Key files; agsync block added. -->
